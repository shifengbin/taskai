# task-list-scrolling Specification

## Purpose
TBD - created by archiving change fix-task-list-scrolling. Update Purpose after archive.
## Requirements
### Requirement: 各任务状态列表在固定面板内可滚动
系统 MUST 将左侧任务面板中的工具栏、状态标签和任务列表限制在应用可用视口高度内。未执行、执行中和已完成状态下的任务列表 MUST 各自在状态标签下方的剩余空间内垂直滚动，且不得被窗口裁切或推动应用根容器滚动。

#### Scenario: 未执行任务超出可见高度
- **WHEN** 未执行标签中的任务条目总高度超过任务列表可用高度
- **THEN** 用户可以在列表区域滚动到最后一个未执行任务，任务面板工具栏和状态标签保持在其各自区域内

#### Scenario: 切换到任务数量较多的状态
- **WHEN** 用户从当前标签切换到执行中或已完成标签，且目标状态的任务条目超出可见高度
- **THEN** 目标状态的任务列表在同一面板剩余空间内可独立垂直滚动并展示全部任务

### Requirement: 隐藏任务列表滚动条但保留滚动交互
系统 MUST 不显示且不得为任务列表预留原生滚动条空间。任务列表 MUST 继续响应鼠标滚轮、触控板和键盘产生的原生垂直滚动。

#### Scenario: 无可见滚动条浏览长列表
- **WHEN** 任一状态的任务列表内容超出可见高度
- **THEN** 列表不显示可见的原生滚动条，且用户仍可使用滚轮或触控板滚动查看下方任务

### Requirement: 保持既有任务树交互
系统 MUST 在可滚动列表中保留任务选择、任务与终端展开、任务操作及同状态任务拖动排序的既有行为。

#### Scenario: 滚动后操作列表中的任务
- **WHEN** 用户滚动到初始不可见的任务条目并选择、展开终端或执行既有任务操作
- **THEN** 对应操作与任务位于初始可见区域时的行为一致

### Requirement: 滚动后打开的任务操作菜单保持在视口内

系统 MUST 在任务列表滚动后的任意可见任务上，为“任务操作”下拉菜单和右键菜单计算不超出应用可用视口的展示位置。系统 MUST 优先在触发位置下方展示菜单；下方空间不足时 MUST 将菜单展示在触发位置上方，并在水平方向调整位置以避免越过视口左右边缘。

#### Scenario: 列表底部任务打开下拉菜单
- **WHEN** 用户滚动到任务列表靠下的位置，并点击剩余下方空间不足以展示完整菜单的“任务操作”按钮
- **THEN** 菜单展示在按钮上方或其他可用位置，所有菜单项均保留在视口内并可选择

#### Scenario: 列表底部任务打开右键菜单
- **WHEN** 用户在任务列表靠下的位置右键点击任务，且点击位置下方空间不足以展示完整菜单
- **THEN** 菜单展示在点击位置上方或其他可用位置，所有菜单项均保留在视口内并可选择

#### Scenario: 菜单在视口右边缘打开
- **WHEN** 用户在靠近视口右边缘的位置打开任务操作菜单或右键菜单
- **THEN** 系统调整菜单的水平位置，使菜单不越过视口右边缘且不遮挡其可访问性

### Requirement: 高度受限的任务操作菜单保留全部操作

当完整任务操作菜单的高度超过任何一侧可用视口高度时，系统 MUST 限制菜单高度并提供菜单内部纵向滚动。系统 MUST 不裁切菜单项，也不得因此使任务列表或应用根容器滚动。

#### Scenario: 菜单项目数量超过可用高度
- **WHEN** 用户打开包含超过可用视口高度的任务操作菜单
- **THEN** 菜单在视口内展示，并允许用户在菜单内部滚动到每一项操作

### Requirement: 视口定位不改变任务操作语义

系统 MUST 在调整任务菜单位置后保持既有的菜单项目过滤、生命周期锁定、菜单项回调、点击外部关闭和 Escape 关闭行为。

#### Scenario: 从翻转后的菜单执行操作
- **WHEN** 用户从因视口空间不足而翻转或限制高度的任务菜单中选择一个可用操作
- **THEN** 系统执行与同一菜单在普通位置打开时相同的任务操作，并关闭菜单

