## 1. 右侧终端标题栏

- [ ] 1.1 调整 `TerminalView`：移除右侧实时状态点和关闭终端按钮，只保留活动终端的快捷输入入口。
- [ ] 1.2 将快捷输入入口从上下文操作显隐样式中分离，确保其在支持和不支持悬停的输入设备上始终可见，并保持标题可裁剪布局。
- [ ] 1.3 保持左侧 `TaskTree` 中终端状态点、关闭入口及其现有悬浮、焦点和无悬停设备行为不变。

## 2. 回归测试与验证

- [ ] 2.1 更新 `TerminalView` 组件测试，验证右侧标题栏存在快捷输入入口且不渲染实时状态点、关闭终端按钮或上下文操作显隐样式。
- [ ] 2.2 运行受影响的 `TerminalView` 与 `TaskTree` 前端测试，确认快捷输入、左侧状态和关闭终端行为未回归。
- [ ] 2.3 运行前端构建，确认类型检查与样式构建通过。
