## 1. 开发准备

- [x] 1.1 在任务目录的 `.worktrees` 创建终端外观功能分支工作树，并保留当前工作区的既有改动。

## 2. 设置模型与迁移

- [x] 2.1 为完整终端主题默认值、逐项归一化以及旧设置迁移补充 Go 单元测试。
- [x] 2.2 在 `internal/settings` 定义终端主题设置、暗色默认主题和颜色归一化，并在 `internal/storage` 加载流程中持久化迁移结果。
- [x] 2.3 重新生成 Wails 前端绑定，并更新前端设置类型与 API 载荷以传递完整终端主题。

## 3. 终端会话更新

- [x] 3.1 为保存后主题和字体更新已有会话、保留输出和输入、重新适配可见终端编写终端会话与终端视图测试。
- [x] 3.2 将 xterm 主题解析改为独立终端主题，移除对工作台颜色模式的依赖，并实现主题、字体、字号的一次性保存后会话更新。
- [x] 3.3 在字体或字号变更后重新适配当前可见终端并同步 PTY；确保后台会话在下次显示时适配。

## 4. 设置界面

- [x] 4.1 为终端配色草稿、仅颜色选择器操作、选区透明度、预览、恢复默认、取消不生效和保存后即时更新补充前端测试。
- [x] 4.2 在“工作区与外观”实现终端外观区域：基础颜色、常规 ANSI、高亮 ANSI、选区透明度、草稿预览和恢复默认操作。
- [x] 4.3 保留字体与字号控件的草稿语义，并在保存成功后与终端主题一起更新现有会话。

## 5. 规格与交付验证

- [x] 5.1 确认实现与 `terminal-appearance-settings`、`terminal-font-selection` 和更新后的视觉规格一致。
- [x] 5.2 将当前分支合并到工作树功能分支，运行相关 Go 与前端测试、`go test -race ./...`、`npm test` 和 `npm run build`。
- [ ] 5.3 使用 `scripts` 中的编译脚本完成项目编译；验证通过后将工作树功能分支合并回当前分支。
