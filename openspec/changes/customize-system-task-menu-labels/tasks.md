## 1. 开发准备与设置模型

- [x] 1.1 按项目协作约定在 `.worktrees/` 创建独立工作树和开发分支，确认工作树基线干净。
- [x] 1.2 在 `internal/settings` 和 `internal/storage` 先编写失败测试，覆盖四个默认系统菜单项、系统名称保留、搁置两种名称、空白名称拒绝、不可变行为字段规范化和旧三项配置加载补齐。
- [x] 1.3 扩展 Go 菜单项类型、稳定系统 ID、默认列表和规范化逻辑，使搁置切换成为受保护的系统项并仅保存允许编辑的名称。
- [x] 1.4 更新前端菜单类型和默认配置；重新生成 Wails 绑定并检查生成类型包含新增字段。
- [x] 1.5 运行 `go test ./internal/settings`，确认设置模型和兼容行为通过。

## 2. 菜单管理界面

- [x] 2.1 在 `frontend/src/App.test.tsx` 先编写失败测试，覆盖普通系统项的名称编辑、搁置项的两种名称编辑、系统项不显示命令配置或删除控件、排序和保存载荷。
- [x] 2.2 调整菜单管理列表，使所有系统项均可进入受限编辑界面，并清楚标示固定操作不可修改。
- [x] 2.3 按菜单项类型收敛编辑表单：普通系统项只编辑一个名称，搁置项编辑两种名称，自定义命令保留现有字段和删除能力。
- [x] 2.4 运行 `cd frontend && npm test -- --run src/App.test.tsx`，确认设置界面通过。

## 3. 任务菜单渲染与交互

- [x] 3.1 在 `frontend/src/components/TaskTree.test.tsx` 先编写失败测试，覆盖搁置项按保存顺序显示、两种状态名称和图标、点击调用既有搁置回调，以及非执行中和生命周期锁定任务不显示该项。
- [x] 3.2 将任务树中独立追加的搁置菜单项改为统一的配置驱动渲染；按任务搁置状态选择名称和现有图标。
- [x] 3.3 保持右键菜单与“任务操作”下拉菜单共用顺序、菜单定位和高度限制，以及既有任务状态过滤规则。
- [x] 3.4 运行 `cd frontend && npm test -- --run src/components/TaskTree.test.tsx src/App.test.tsx`，确认菜单行为通过。

## 4. 文档、验证与交付

- [x] 4.1 更新 README，说明系统菜单项可改名、搁置切换可排序且有两种名称。
- [x] 4.2 运行 `openspec validate customize-system-task-menu-labels --strict`、`go test -race ./...`、`cd frontend && npm test && npm run build` 和 `git diff --check`。
- [x] 4.3 按项目流程将开发变更合并至工作树分支，使用 `scripts/` 中的 Linux 构建脚本编译可执行程序并打开应用进行确认。
- [ ] 4.4 等待用户确认后，将工作树分支合并回当前项目分支，再次编译验证合并结果。
- [ ] 4.5 同步中文文档、归档 OpenSpec 变更并提交 Git 变更。
