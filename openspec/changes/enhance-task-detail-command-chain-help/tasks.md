## 1. 生命周期输入快照后端

- [ ] 1.1 抽取生命周期首段输入的共享构造路径，并新增按任务 ID 返回当前输入 JSON 的只读应用绑定。
- [ ] 1.2 让生命周期实际执行复用该构造路径，更新 Wails 生成绑定与前端 API 包装，避免复制与执行的 JSON 语义漂移。
- [ ] 1.3 为额外信息、活动终端、模板字段、`baseURL` 存在/缺失及任务不存在补充 Go 测试。

## 2. 任务详情命令上下文

- [ ] 2.1 在任务详情中按分类渲染额外信息快照的名称、固定字段和动态参数，并提供复选框参数和无额外信息的可读状态。
- [ ] 2.2 添加只包含 `TASKAI_TASK_ID`、`TASKAI_TERMINAL_ID`、`TASKAI_STATUS_API` 的系统环境变量说明区及其作用范围。
- [ ] 2.3 添加“复制当前命令链输入 JSON”操作，调用只读绑定并使用 Wails 剪贴板 API 显示成功或失败反馈。
- [ ] 2.4 扩展前端测试，覆盖额外信息、系统变量排除模板变量、复制成功及复制失败交互。

## 3. 命令链扩展规则帮助

- [ ] 3.1 在“设置 → 生命周期编排”的命令链区域添加帮助入口和中文对话框。
- [ ] 3.2 编写帮助内容，覆盖钩子范围、固定与链级追加参数、标准输入输出、环境变量范围、内置命令特殊参数和从链首重试。
- [ ] 3.3 扩展前端测试，验证帮助入口、关闭交互及参数与管道规则文本。

## 4. 验证

- [ ] 4.1 运行受影响的 Go 测试及前端 Vitest 测试，修复本变更引入的问题。
- [ ] 4.2 运行 `scripts/build-linux.sh` 完成 Linux 应用编译验证。
