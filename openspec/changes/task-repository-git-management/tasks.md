## 1. 后端仓库发现与状态读取

- [x] 1.1 定义任务仓库 Git 的传输类型和内部服务契约，包含相对路径、分支、远程、改动、上游和主操作状态。
- [x] 1.2 实现并测试任务工作目录的真实路径边界校验、仓库相对路径校验和 Git worktree 验证。
- [x] 1.3 实现并测试递归发现普通仓库、嵌套仓库和 Git worktree，跳过 `.git` 与符号链接目录并按顶层路径去重。
- [x] 1.4 实现并测试仓库状态读取，包括未提交改动、当前分支、远程选择、远程同名分支和本地上游状态。
- [x] 1.5 实现并测试最大扫描深度：任务目录为第 1 层，默认只扫描到第 2 层且不进入更深目录。
- [x] 1.6 读取远程、本地跟踪引用和当前分支的提交差异，标识完全同步的仓库。

## 2. 受控 Git 操作

- [x] 2.1 实现并测试提交操作：校验非空提交信息，执行 `git add -A` 与 `git commit -m`，并在结果中返回刷新后的状态或可读错误。
- [x] 2.2 实现并测试发布分支操作：重新校验工作区、选择远程并执行 `git push -u <remote> <branch>`。
- [x] 2.3 实现并测试同步操作：重新校验工作区，必要时建立同名上游，并按 `pull` 后 `push` 的顺序执行；拉取失败时不得推送。
- [x] 2.4 为无远程、多远程无 `origin`、无当前分支、认证失败、网络失败和冲突错误提供可展示的中文错误，并确保失败后状态刷新。

## 3. Wails 应用接口

- [x] 3.1 在 `App` 和应用绑定契约中新增列出仓库、提交、发布分支和同步远程方法，使用任务 ID 与仓库相对路径作为输入。
- [x] 3.2 在应用层复用任务工作目录与生命周期锁定校验，禁止前端传入任意 Git 命令、路径或远程参数。
- [x] 3.3 生成 Wails 前端绑定并检查 `frontend/wailsjs/` 的导出类型和方法变更。
- [x] 3.4 从持久化设置读取 Git 最大扫描深度并传给仓库发现服务。

## 4. 任务详情 Git 界面

- [x] 4.1 扩展前端 API 和类型，映射任务仓库 Git 状态及四个后端方法。
- [x] 4.2 将现有任务详情拆分为“项目信息”和“Git”标签，保留项目信息既有内容和终端优先显示行为。
- [x] 4.3 实现 Git 标签的加载和工作目录/无仓库空态。
- [x] 4.4 实现每个仓库独立的卡片、提交信息输入、请求中状态、错误反馈以及状态驱动的“提交”“发布分支”“同步远程”单一主按钮。
- [x] 4.5 在操作完成或失败后只刷新对应仓库，确保一个仓库的状态不覆盖其他仓库。
- [x] 4.6 在“工作区与外观”设置中提供并保存范围为 1–10 的 Git 最大扫描深度，默认显示 2。
- [x] 4.7 已同步时禁用“同步远程”按钮；没有工作目录或目录尚未创建时隐藏 Git 标签并避免请求仓库列表；从 Git 标签切换到这类任务时自动显示项目信息。

## 5. 验证与文档

- [x] 5.1 为 Go 服务和应用接口添加覆盖发现、边界校验、远程选择、提交、发布与同步失败路径的测试。
- [x] 5.2 为前端任务详情添加标签、状态驱动按钮、独立仓库状态和错误反馈的测试。
- [x] 5.3 运行受影响的 Go 与前端测试，再运行 `go test -race ./...`、`npm test` 和 `npm run build`。
- [x] 5.4 使用 `wails dev` 进行集成验证，检查多仓库发现、提交、发布和同步流程；随后按项目构建脚本编译并打开应用。
- [ ] 5.5 将最终实现与规格同步，归档 OpenSpec 变更并更新必要的中文文档。
