## 1. 固定命令与预置迁移

- [x] 1.1 在生命周期设置模型中新增“更新默认分支”的固定 ID、命令类型、中文说明、允许钩子和 `templateField` 链级参数规范化/解析，并覆盖无参数、指定字段及无效参数测试。
- [x] 1.2 将预置版本升级至 4，使用版本 1、2、3 的静态历史预置定义完成迁移，并仅为完全匹配版本 3 的 `iterations-ai` 与“更新仓库”链首插入新命令。
- [x] 1.3 更新新安装默认命令链与设置、存储测试，覆盖新顺序、删除或修改预置链不被恢复、早期预置迁移仍正确执行及默认链映射不变。

## 2. 执行态默认分支

- [x] 2.1 扩展生命周期调度请求，冻结活动模板的可见字段，并在命令链运行器中深复制可变的任务 Git 信息执行态。
- [x] 2.2 实现“更新默认分支”执行逻辑：解析默认或指定字符串字段、保留显式 Git 分支、补全空内置 Git 分支、保存单仓库克隆的执行态分支，并保持系统命令字节流不变。
- [x] 2.3 调整“克隆指定 Git 仓库”、Git 仓库克隆和清单生成仅消费执行态分支，移除其模板字段回退；补充命令顺序、空值、非字符串字段、显式分支及未加入新命令的回归测试。
- [x] 2.4 移除任务创建和编辑时的模板分支持久化回填，更新服务测试以验证保存后 Git 信息保持用户提交值且生命周期运行不回写快照。

## 3. 界面与文档

- [x] 3.1 更新 Wails/前端生命周期命令类型、命令链编辑交互与前端测试，展示“更新默认分支”及 `templateField=<模板字段键>` 的中文说明。
- [x] 3.2 更新 README 生命周期命令、默认预置链和分支语义文档，明确执行态作用域、显式分支优先、未配置更新命令时的远程默认分支行为及迁移边界。

## 4. 验证

- [x] 4.1 运行 `gofmt`、相关 Go 测试和 `go test ./...`，修复本变更引入的失败。
- [x] 4.2 在 `frontend` 目录运行 `npm test` 与 `npm run build`，确认绑定类型和生命周期编排交互通过。
- [x] 4.3 运行 `./scripts/build-linux.sh`、`openspec validate extract-default-branch-lifecycle-command --strict` 与 `git diff --check`，记录验证结果。
