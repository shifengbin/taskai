## 1. 交互操作目录解析

- [x] 1.1 在应用层增加共享的执行中任务操作上下文，保留状态与生命周期锁定校验，并在生命周期链选择为空时安全解析当前用户的 Home 目录。
- [x] 1.2 保持已选择任意命令链任务的持久化工作目录语义，并使 Home 目录查询与可用性校验可在测试中替换。
- [x] 1.3 将 Shell 终端、命令终端、直接任务命令、打开任务文件夹和任务菜单自定义命令统一接入该操作目录；确保菜单前置和后置脚本复用同一目录。

## 2. 自动化测试

- [x] 2.1 覆盖空链执行中任务在不创建任务目录的情况下启动 Shell 终端、命令终端和打开 Home 目录。
- [x] 2.2 覆盖空链执行中任务的直接命令、任务菜单主命令及前置和后置脚本均在 Home 目录运行。
- [x] 2.3 覆盖 Home 目录查询失败、不可访问或不是目录时操作被拒绝，且不会启动部分操作。
- [x] 2.4 覆盖已选命令链且任务目录不可用时继续报告既有错误，绝不回退到 Home。

## 3. 验证

- [x] 3.1 运行受影响的 Go 应用层测试，并检查任务工作区快照和生命周期目录清理逻辑未被改变。
- [x] 3.2 运行 `go test -race ./...`、`cd frontend && npm test && npm run build`，再使用 `scripts/` 中适用的编译脚本完成构建验证。
