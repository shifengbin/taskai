## 1. 删除契约与后端校验

- [x] 1.1 在 `.worktrees/` 创建 `worktree/add-pending-task-deletion` 工作树分支并在其中开发，确认目标分支工作区干净。
- [x] 1.2 为生命周期服务新增失败测试，覆盖未执行与已完成任务的混合成功删除、执行中或存在生命周期执行记录时的原子拒绝，以及不触碰保留工作目录。
- [x] 1.3 将 `DeleteCompletedTasks` 重命名为 `DeleteTasks`，在生命周期服务中保持全量校验和单次持久化，并将可删除状态扩展为 `pending` 和 `completed`。
- [x] 1.4 更新应用层绑定和测试，保留确定性的生命周期锁与删除成功后的实时状态清理。

## 2. 前端选择与确认交互

- [x] 2.1 更新 Wails 前端绑定和 `frontend/src/api.ts`，暴露类型化的 `deleteTasks` 调用，并补充 API 封装测试。
- [x] 2.2 为 `TaskTree` 补充失败测试，覆盖未执行任务选择模式、逐项选择、全选、切换标签或取消后清空选择，以及带生命周期执行记录的任务不可选。
- [x] 2.3 将 `App` 和 `TaskTree` 的已完成任务专用选择状态泛化为当前标签的任务删除选择状态；在未执行和已完成标签显示相同的工具栏、复选框与确认入口。
- [x] 2.4 更新确认文案和应用测试，明确“仅删除任务记录、不影响工作目录”，并覆盖成功删除后列表、计数、详情、终端、展开状态和本地会话的清理，以及服务端拒绝时保留选择。

## 3. 文档与交付验证

- [x] 3.1 更新 `README.md` 中的任务删除说明，使其覆盖未执行和已完成任务的选择、资格限制及仅删除记录的行为。
- [x] 3.2 运行受影响的 Go 和前端测试，然后运行 `go test -race ./...`、`cd frontend && npm test && npm run build`。
- [x] 3.3 使用 `./scripts/build-linux.sh` 构建可执行程序并启动 `build/bin/taskai`，确认删除交互正常且启动时不禁用终端颜色。
- [ ] 3.4 在用户确认后，将工作树分支合并回目标分支，重新构建验证、同步并归档 OpenSpec 文档、提交变更并移除已合并工作树。
