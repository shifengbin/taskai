## 1. 后端任务搁置能力

- [x] 1.1 为任务模型增加持久化的搁置标记，并补充旧数据默认未搁置及状态转换清除标记的单元测试。
- [x] 1.2 在生命周期服务中实现执行中任务的原子搁置状态切换：校验状态与生命周期锁定、维护正常与搁置分组顺序、返回完整任务列表，并补充服务测试。
- [x] 1.3 扩展应用契约和 Wails 绑定，暴露搁置状态切换；在应用层执行锁定校验，并补充应用层测试。

## 2. 前端任务树展示与操作

- [x] 2.1 更新前端任务类型、API 包装和 Wails 生成绑定类型，使其支持搁置字段与完整列表返回值。
- [x] 2.2 在应用层调用搁置切换绑定，使用返回的完整任务列表刷新状态，并沿用现有错误提示处理失败情形。
- [x] 2.3 重构任务树的执行中列表渲染：保留正常任务顶层条目，在末尾增加默认收起的同级“已搁置（n）”折叠区，并确保搁置区不参与正常任务拖动排序。
- [x] 2.4 在任务操作菜单中按任务搁置状态展示“搁置任务”或“取消搁置”，并为折叠区内任务保留既有终端与任务操作。
- [x] 2.5 补充任务树和应用层前端测试，覆盖默认收起、展开/收起、菜单切换、取消搁置追加末尾、持久化列表刷新和错误回退。

## 3. 文档与验证

- [x] 3.1 更新 `README.md`，说明执行中任务的搁置折叠区、状态不变语义及取消搁置后的排序规则。
- [x] 3.2 运行 `go test -race ./...`、`cd frontend && npm test -- --run`、`npm run build` 和 `openspec validate add-running-task-shelving --strict`。
- [x] 3.3 运行 `./scripts/build-linux.sh`，确认项目编译成功并生成 `build/bin/taskai`。
