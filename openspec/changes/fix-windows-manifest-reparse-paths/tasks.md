## 1. Windows 工作目录验证

- [x] 1.1 调整 Windows 清单写入器的工作目录打开流程，允许解析上级重解析点，并验证最终任务工作目录是非重解析点目录。
- [x] 1.2 保持已验证目录句柄以下的相对路径拒绝策略，并为最终目录不安全或不可用的情形返回明确的生命周期错误。

## 2. Windows 回归测试

- [x] 2.1 增加 Windows 专属测试，验证位于目录联接点等上级重解析点下的普通任务工作目录可以生成清单。
- [x] 2.2 增加或扩展测试，验证最终任务工作目录、输出子目录和清单目标为重解析点时仍被拒绝且不会写到目录外。

## 3. 验证

- [x] 3.1 在 Windows 上运行生命周期与应用层相关 Go 测试，确认清单生成、重试和失败语义保持不变。
- [x] 3.2 运行项目完整 Go 测试套件，并确认非 Windows 平台的清单路径安全测试仍通过。
