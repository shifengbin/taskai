## 1. 终端状态快照

- [x] 1.1 将 `TerminalSessionRegistry` 的活动画面快照改为从活动缓冲区 `baseY` 读取，不再把用户滚动位置纳入快照或相等性比较。
- [x] 1.2 移除仅为滚动重建状态基线而存在的监听与清理逻辑，保留尺寸适配和外观更新时的基线重置。

## 2. 回归测试

- [x] 2.1 扩展终端会话测试桩以表达 `baseY` 与 `viewportY` 的独立位置。
- [x] 2.2 添加用户向上滚动后活动终端页单元格变化仍上报活动的回归测试。
- [x] 2.3 添加用户滚动或历史区域变化、活动终端页不变时不报告活动的回归测试。

## 3. 验证

- [x] 3.1 运行受影响的终端会话 Vitest 测试。
- [ ] 3.2 运行 `cd frontend && npm test -- --run && npm run build`。
- [ ] 3.3 运行 `go test -race ./...`。
- [ ] 3.4 运行 `./scripts/build-linux.sh`，使用 `wails dev` 手动验证向上滚动终端历史时的输出状态，并启动编译后的程序验证。
