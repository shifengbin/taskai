## 1. 视觉基础与保护边界

- [x] 1.1 检查并保留工作区已有的未提交改动，确认本变更只触及前端视觉文件且不覆盖既有业务工作。
- [x] 1.2 在 `App.tsx` 的 MUI 主题中建立松林夜跑亮色/暗色语义令牌、状态色、`contrastText`、形状、排版和组件默认覆盖，继续使用现有 `colorScheme`。
- [x] 1.3 在 `App.css` 与 `style.css` 建立对应 CSS 变量、松林画布、反光条纹、焦点环、图标容器和减少动态效果规则，并清理视觉范围内的旧固定色值。
- [x] 1.4 为亮色和暗色主要文字、按钮、状态标签及表单标签计算并记录至少 4.5:1 的实际对比度组合。

## 2. 工作台与任务导航

- [x] 2.1 重塑启动屏、顶栏、工具按钮、可拖拽分隔条、详情空状态与任务详情容器，保留现有布局、事件处理与操作位置。
- [x] 2.2 重塑 `TaskTree` 的状态标签、任务行、终端子项、选中态、悬停/焦点/禁用态、任务自定义颜色承载方式和任务开始反馈。
- [x] 2.3 将任务拖拽预览与插入指示器改为清晰的赛道视觉反馈，同时保留现有指针阈值、放置判定和排序回调。
- [x] 2.4 适配 `TerminalStatusDot` 的空闲、工作中、未读和错误颜色及脉冲，并保持其既有角色、标签和减少动态效果行为。

## 3. 终端与覆盖层

- [x] 3.1 让 `TerminalView` 从当前 MUI 主题读取并更新 xterm 的背景、前景、光标和选择色；不改动输入输出、尺寸、焦点、剪贴板或终端生命周期逻辑。
- [x] 3.2 通过 MUI 组件覆盖统一任务编辑、额外信息管理/编辑、模板编辑、设置、状态帮助、菜单项编辑、结束确认和退出确认对话框的标题带、内容区和操作栏。
- [x] 3.3 统一 Menu、Popover、Snackbar、Alert、Accordion、Tabs、Chip、TextField、Checkbox、Switch 和危险操作的松林夜跑外观与可见焦点，不改变其打开、关闭、提交或校验行为。
- [x] 3.4 添加克制的启动、弹窗、状态和拖拽动效，并在 `prefers-reduced-motion: reduce` 下提供无动画的等价静态反馈。

## 4. 行为回归与交付验证

- [x] 4.1 更新或补充前端测试，覆盖亮暗主题入口、任务树和终端状态的既有无障碍语义，以及视觉改造后不变的关键交互。
- [x] 4.2 离线检查亮色、暗色、任务详情、空状态、活动终端、拖拽、错误提示与每类既有弹窗的视觉层级、可读性和减少动态效果表现。
- [x] 4.3 运行前端测试与项目规定的 `scripts/build-linux.sh`，记录结果并仅修复本视觉变更引入的问题。

## 5. 用户反馈修订

- [x] 5.1 移除终端标题栏中与图标和标题重叠的横杠装饰，并让亮色与暗色模式的终端都保持深色高对比表面。
- [x] 5.2 让任务详情区域占满右侧可用宽度，移除干扰阅读的背景图，并提高暗色模式的栏目和项目标题对比度。
- [x] 5.3 为反馈修订补充回归测试，执行前端测试、严格 OpenSpec 校验和项目构建脚本。

## 6. 长任务表单可用性

- [x] 6.1 让新建和编辑任务对话框在受限视口高度内使用独立内容滚动区，并始终保留可操作的取消与创建或保存按钮。
- [x] 6.2 添加回归测试，运行前端测试、严格 OpenSpec 校验和项目构建脚本。

## 7. 选中任务尾标精简

- [x] 7.1 移除选中任务右上角的黄绿斜纹尾标，保留任务颜色、选中态与拖拽插入提示。
- [x] 7.2 添加回归测试，运行前端测试、严格 OpenSpec 校验和项目构建脚本。
