## 1. 回归测试

- [x] 1.1 在应用层测试中复现“终端自然退出并变为异常后，用户关闭已退出终端”，断言终端实时记录被移除且任务聚合状态恢复为 `idle`。
- [x] 1.2 覆盖关闭异常终端但任务仍有其他终端的情形，断言任务状态按剩余终端的最高优先级聚合。
- [x] 1.3 覆盖活动终端的正常关闭与关闭失败，断言正常关闭保持幂等且失败时不清理实时状态。
- [x] 1.4 在前端状态测试中验证接收带 `terminalRemoved` 的新版本事件后，任务级异常状态被更新，已移除终端不会重新出现在任务树中。

## 2. 实时状态清理

- [x] 2.1 修改 `App.CloseTerminal`：仅在终端管理器成功关闭或确认已关闭后，调用现有实时状态服务移除终端；重复移除不得作为错误返回。
- [x] 2.2 保持终端自然退出时的原始退出原因与首次异常状态不变，不新增持久化字段、Wails API 或前端直接状态覆盖。
- [x] 2.3 确认现有版本化实时状态事件能将移除后的任务聚合状态投影到收起任务行。

## 3. 验证与交付

- [x] 3.1 运行受影响的 Go 包和前端状态测试，确认异常退出、手动关闭和多终端聚合场景通过。
- [x] 3.2 运行 `go test -race ./...`、`cd frontend && npm test && npm run build`。
- [x] 3.3 使用 `scripts/build-linux.sh` 构建可执行程序并启动应用，手动确认关闭异常终端后收起任务不再显示红色状态点；启动时不得禁用终端颜色。
- [ ] 3.4 将实现结果与本 change 的规格、设计和任务同步；按项目流程等待确认后再合并、提交并归档 change。
