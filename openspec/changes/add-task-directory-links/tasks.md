## 1. 开发隔离与模板数据模型

- [x] 1.1 在任务目录的 `.worktrees` 中创建功能 worktree 和分支，并确认 OpenSpec 工件及当前测试基线可用
- [x] 1.2 先增加模板字段归一化测试，覆盖 `directories`、`multiple`、缺失 `updatable` 向 `true` 兼容以及目录字段禁用默认值和环境变量
- [x] 1.3 扩展 `TaskTemplateField`、任务模板值归一化和 Wails 类型，使目录值统一保存为 `[]string`
- [x] 1.4 先增加模板变更校验测试，覆盖已使用字段类型变更、多目录改为单目录以及必填空值同时锁定的拒绝规则
- [x] 1.5 实现基于已有任务数据的模板变更校验，并保证校验失败不产生部分设置保存
- [x] 1.6 先增加任务创建和更新测试，覆盖绝对目录、存在性、可访问性、任务范围重复、必填、单目录数量及所有状态的锁定字段比较
- [x] 1.7 实现目录值校验与不可更新字段后端保护，确保 running 任务校验失败时不保存其他字段也不调度 `updateTask`
- [x] 1.8 扩展任务绑定模板字段解析、HTTP 响应和生命周期输入 JSON 测试，验证目录数组可见、历史目录字段隐藏且不生成环境变量

## 2. 目录链接规划与安全同步

- [x] 2.1 先为链接规划器增加测试，覆盖 basename、父目录逐级消歧、大小写冲突、集合变化重命名、重复来源和无法命名
- [x] 2.2 实现从任务绑定模板目录字段生成确定性期望链接的纯规划逻辑，并让错误包含字段和来源路径
- [x] 2.3 定义不跟随来源内容的目录链接平台接口，并增加 Unix symlink 与 Windows symlink/junction 的平台测试或构建测试
- [x] 2.4 实现 Unix 与 Windows 目录链接适配器，确保 Windows 操作不启动可见命令窗口且不支持时明确失败
- [x] 2.5 先增加事务清单测试，覆盖首次同步、空期望、增加、替换、删除、pending 恢复、清单损坏和重复执行幂等
- [x] 2.6 在 `.taskai-ownership` 安全边界内实现按任务所有权令牌隔离的原子 pending/稳定目录链接清单
- [x] 2.7 先增加冲突和删除安全测试，覆盖普通文件、普通目录、未登记链接、被替换登记链接及来源目录永久保留
- [x] 2.8 实现同步预检和收敛，只创建期望链接、只删除目标仍匹配的登记链接，并在任何冲突时保留未知条目
- [x] 2.9 扩展受控工作目录删除测试与实现，在删除成功后清理链接清单，并证明 `RemoveOwned` 不跟随目录链接删除外部来源

## 3. 生命周期命令、默认链与迁移

- [x] 3.1 先增加固定系统命令测试，覆盖 `system.lifecycle.sync-directory-links` 的名称、命令类型、三个适用钩子、无参数和不可编辑删除
- [x] 3.2 注册同步目录链接命令类型并接入生命周期执行器，成功时原样传递标准输入，失败时返回包含字段和路径的错误
- [x] 3.3 先增加系统默认链测试，覆盖创建链、`iterations-ai`、更新仓库链、同步专用链和保持不变的删除链顺序
- [x] 3.4 更新系统链和默认预设定义，使默认预设获得 `updateTask` 同步链，公司框架继续通过更新框架仓库链同步
- [x] 3.5 先构造上一版本系统链、已修改系统链、自定义链、预设和任务选择快照，覆盖只迁移完全匹配系统形状的规则
- [x] 3.6 提高预设迁移版本并实现命令、链、预设和旧默认任务选择迁移，确保用户自定义配置逐字段保持不变
- [x] 3.7 增加生命周期集成测试，验证 pending 在启动时同步、`beforeStart` 失败阻止启动、running 先保存再同步以及失败后使用最新值重试

## 4. 模板设置与任务表单

- [x] 4.1 建立可替换的 Wails 原生目录选择适配器并增加前端测试，确保取消选择不改变现值且测试可注入临时绝对路径
- [x] 4.2 扩展模板编辑器，增加“目录”类型及多目录、必填、创建后更新开关，并隐藏目录字段的默认值和环境变量设置
- [x] 4.3 扩展任务字段类型和状态初始化，让单目录与多目录均使用字符串数组并兼容旧字符串和布尔字段
- [x] 4.4 实现单目录只读路径行及重新选择、移除操作，确保页面不渲染可手工输入路径的控件
- [x] 4.5 实现多目录列表及逐次添加、重新选择、移除、重复提示和完整路径展示
- [x] 4.6 在创建成功后按任务绑定模板锁定不可更新字段，覆盖绑定模板策略变化，并确保切换全局当前模板不能改变既有任务绑定
- [x] 4.7 将后端字段、错误和目录数组接入任务创建更新调用，重新生成 Wails bindings 并检查生成的 TypeScript 接口差异
- [x] 4.8 增加前端组件测试，覆盖单/多目录、必填提示、锁定、后端拒绝信息和原生选择器取消行为

## 5. 自动化验证与集成验收

- [x] 5.1 运行受影响 Go 包的单元和 race 测试，修复模型、设置、storage、workspace 与 lifecycle 的失败
- [x] 5.2 运行前端测试和生产构建，确认 TypeScript、目录选择适配器和任务表单均通过
- [x] 5.3 运行 `go test -race ./...`、`frontend` 下的 `npm test` 与 `npm run build`，记录完整验证结果
- [x] 5.4 用临时 `project-a/src`、`project-b/src` 和任务工作目录执行真实链接集成测试，核对名称、独立目标、幂等、冲突保护和删除后来源完整性
- [x] 5.5 使用 `wails dev` 启动应用，从输出取得调试地址，并用 chrome-devtools 测试模板配置、单/多目录表单、创建后锁定、添加移除和错误展示
- [x] 5.6 在浏览器测试中替换目录选择适配器返回临时路径，并另执行一次真实原生目录选择器 smoke test；测试完成后关闭调试进程
- [x] 5.7 使用 `scripts` 中的编译脚本生成可执行程序并打开应用，等待用户确认功能和界面验收结果

## 6. 合并、文档与归档

- [ ] 6.1 用户确认后将当前工作区的必要变更合并到功能 worktree 分支，解决冲突并重新运行相关集成测试
- [ ] 6.2 将功能 worktree 分支合并到当前项目分支，并再次编译验证合并结果
- [ ] 6.3 同步中文规格和 `docs/plans` 实施记录，核对实际行为、迁移版本及集成测试步骤与 OpenSpec 一致
- [ ] 6.4 归档已完成的 OpenSpec change，提交全部代码和文档变更，并确认工作树干净
- [ ] 6.5 移除已合并的功能 worktree，保留最终分支和验证记录
