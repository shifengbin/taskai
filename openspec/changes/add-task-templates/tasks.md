## 1. 任务模板数据与持久化

- [x] 1.1 在任务领域模型中定义任务模板、字段类型和值映射，并为默认值、必填值、键格式、保留环境变量名和历史值保留规则编写单元测试。
- [x] 1.2 在设置模型中保存模板集合和当前模板标识，完成名称唯一性、字段唯一性、活动模板引用及已使用字段类型不可变更的归一化与测试。
- [x] 1.3 扩展任务创建与更新逻辑，按当前模板合并可见字段、应用缺失字段默认值并保留不可见历史值，补充服务层测试。
- [x] 1.4 扩展存储数据归一化和仓储操作，兼容旧 `tasks.json` 中缺失的模板相关字段，并覆盖加载、保存、模板删除和字段恢复场景。

## 2. 后端接口与生命周期命令链

- [x] 2.1 扩展应用层任务创建、更新和设置保存绑定，使当前模板字段能够经过 Wails API 在前后端之间传递。
- [x] 2.2 在任务 HTTP 列表与详情资源中输出当前模板筛选后的 `templateFields` 对象，并覆盖字符串、布尔值、无模板和历史字段隐藏场景。
- [x] 2.3 让生命周期命令链标准输入复用同一 `templateFields` 资源，并为运行开始时的数据快照编写测试。
- [x] 2.4 为自定义生命周期 Shell 命令生成 `TASKAI_<KEY大写>` 环境变量，覆盖布尔序列化、空字符串、保留变量冲突和内置命令不接收变量的测试。

## 3. 设置与任务表单界面

- [x] 3.1 更新 Wails 生成模型、前端领域类型和 API 包装，表达任务模板定义、当前模板选择和字符串/布尔任务字段值。
- [x] 3.2 在设置弹窗中实现任务模板的新增、编辑、删除、字段排序/属性编辑和当前模板选择，并补充组件交互测试。
- [x] 3.3 在任务对话框的描述与颜色之间实现当前模板字段编辑器，支持默认值、文本/复选框、必填提示和无模板状态。
- [x] 3.4 将任务表单字段值接入创建和更新请求，并覆盖旧任务补齐字段、历史值恢复、必填字符串和必填布尔字段的前端测试。

## 4. 文档与验证

- [x] 4.1 更新项目 README 中的任务 HTTP 扩展字段和自定义生命周期命令环境变量说明。
- [x] 4.2 运行 Go 全量测试、前端全量测试和 `scripts/build-linux.sh`，修复本变更引入的问题并记录结果。
