## 1. 后端删除契约

- [ ] 1.1 在生命周期任务服务新增批量删除已完成任务记录的方法：全量校验空集合、重复 ID、未知 ID、状态和生命周期执行记录，通过后单次持久化并返回最新任务列表。
- [ ] 1.2 为服务新增测试，覆盖成功批量删除、任一目标无效时不发生部分删除，以及删除不触及工作目录或生命周期命令。
- [ ] 1.3 在应用层和任务绑定契约暴露批量删除方法，按确定顺序协调目标任务的生命周期锁并在成功后清理实时任务状态；补充应用层测试。

## 2. 前端绑定与选择模式

- [ ] 2.1 重新生成 Wails 前端绑定，并在 `frontend/src/api.ts` 暴露类型化批量删除调用；补充 API 封装测试。
- [ ] 2.2 在 `App` 增加仅用于“已完成”标签的选择模式、待删除 ID 集合和状态切换清理逻辑，并在侧栏标题区提供进入/退出选择模式的操作。
- [ ] 2.3 扩展 `TaskTree` 以在选择模式中展示可删除任务的复选框、选中数量和全选操作；保留非选择模式的详情选择、拖拽、右键和生命周期失败重试行为。
- [ ] 2.4 新增批量删除确认对话框，明确其仅删除记录且不影响工作目录；确认成功后用接口返回列表替换任务状态，并清理详情选择、终端选择、展开状态和待删除集合。

## 3. 前端验证

- [ ] 3.1 补充任务树组件测试：选择模式范围、逐项选择、全选、退出或切换标签清理，以及生命周期锁定完成任务不可选择。
- [ ] 3.2 补充应用测试：确认前不调用删除接口、取消保留记录、成功删除后的列表/计数/详情状态清理，以及服务端拒绝时保留选择并显示错误。

## 4. 全量验证

- [ ] 4.1 运行 Go 服务与应用测试，确认批量删除的校验、原子性和并发保护有效。
- [ ] 4.2 运行前端测试和生产构建，确认任务树选择模式、确认交互和 Wails 绑定通过类型检查与构建。
- [ ] 4.3 运行 `./scripts/build-linux.sh`，确认桌面应用可完成 Linux 构建。
