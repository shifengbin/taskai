## 1. 隔离开发与目录归属模型

- [x] 1.1 在任务目录的 `.worktrees/` 下创建 `worktree/allow-deleting-failed-pending-tasks` 工作树分支，在工作树中合并当前工作区分支并解决冲突，确认不覆盖用户现有变更。
- [x] 1.2 先为任务模型和存储归一化增加失败测试，覆盖目录归属 `unknown`、`not-created`、`created`，路径快照复制，以及旧版生命周期记录缺少新字段时安全归一化为 `unknown`。
- [x] 1.3 扩展 `LifecycleExecution` 保存开始前工作区根目录、任务目录和三态目录归属，保持 JSON 向后兼容，并通过模型与存储测试。

## 2. 创建命令归属追踪与重试

- [x] 2.1 先为 `internal/workspace` 和命令链执行器增加失败测试，覆盖目标不存在时报告本次新建、已有目录时报告复用、创建失败不报告新建，以及创建后归属回调失败时停止后续命令并安全补偿删除。
- [x] 2.2 调整工作目录创建契约与命令链进度契约，使创建命令仅在本次实际新建目录后同步持久化 `created`，归属写入失败时停止命令链并尝试清理本次新建目录。
- [x] 2.3 先增加应用层失败测试，覆盖 `beforeStart` 后台执行前持久化路径与 `not-created`、创建成功后升级为 `created`、创建前失败保持 `not-created`、应用重启保留归属，以及当前工作区根目录变化后重试仍沿用原路径快照。
- [x] 2.4 更新开始前调度、失败记录和重试继承逻辑，确保 `created` 归属只升不降；运行相关 Go 测试并确认异步进度版本与过期运行保护保持有效。

## 3. 启动失败任务受控删除

- [x] 3.1 先为生命周期服务增加失败测试，覆盖 `pending + beforeStart + failed` 可删除、`created` 先删目录再删记录、`not-created` 完全不调用目录删除、预先存在并被复用的同名目录保持不变、目录不存在幂等成功、`unknown` 与运行中或其他失败钩子原子拒绝。
- [x] 3.2 补充删除错误测试，覆盖路径不匹配、工作区外符号链接和文件系统失败时保留全部任务与失败记录，以及批量清理部分完成后保存或后续清理失败时任务记录仍可重试删除。
- [x] 3.3 在生命周期服务中集中实现删除资格和条件目录清理，复用 `workspace.Remove` 的安全边界，全部必要清理成功后才一次保存剩余任务快照；保持普通未执行与已完成任务只删记录。
- [x] 3.4 更新应用层测试，验证删除与重试共享任务锁、删除成功清理实时状态、后端错误保留任务，并运行受影响的 Go 包与应用测试。
- [x] 3.5 根据独立代码审查补充随机所有权令牌、侧车目录身份凭据和隔离删除，覆盖创建后崩溃恢复、同名目录替换拒绝、真实文件系统清理失败、最终保存失败及清理期间并发新增任务。
- [x] 3.6 根据复审补充目录自身随机所有权标记、私有元数据目录校验和首次并发创建恢复，覆盖原目录删除后同名重建、文件系统身份复用及文件系统检查错误时拒绝误删。
- [x] 3.7 根据平台安全复审补充 macOS 扩展 ACL 校验、Windows 创建时私有 DACL、目录标记能力预检、仓储加载前实例文件锁和 staging 占用恢复；覆盖缺失工作区根目录、探测无残留、第二实例拒绝、占用目录保留及各平台交叉编译。

## 4. 前端选择与确认体验

- [x] 4.1 先为前端删除资格辅助逻辑、`TaskTree` 和 `App` 增加失败测试，覆盖明确归属的开始前失败任务可选、`unknown` 与其他锁定任务不可选、普通浏览仍只开放重试，以及后端拒绝时保留选择和错误信息。
- [x] 4.2 集中前端可删除谓词并更新 `App`、`TaskTree` 的复选框与选择集合过滤，避免继续以是否存在 `lifecycleExecution` 作为唯一条件。
- [x] 4.3 更新删除确认文案，明确普通任务只删除记录，启动失败任务仅清理由 TaskAI 实际新建的工作目录，未创建或复用目录不会处理；运行相关前端测试。

## 5. 文档、集成测试与交付

- [x] 5.1 更新 `README.md`，说明开始前失败任务可放弃删除、目录归属判断、仅清理 TaskAI 实际新建目录及清理失败保留任务的行为。
- [x] 5.2 运行 `go test -race ./...`、`cd frontend && npm test && npm run build`，修改导出类型后运行 `wails generate module` 并检查 `frontend/wailsjs/` 生成绑定差异。
- [x] 5.3 使用 `wails dev` 启动应用且不禁用终端颜色、不自动退出；从输出获取调试地址后使用 MCP Chrome DevTools 创建包含“创建任务工作目录→故意失败命令”的开始前链，启动任务并确认其保持未执行、显示失败与重试，同时可进入删除选择。
- [x] 5.4 在浏览器中确认删除该失败任务后任务记录消失、对应 TaskAI 新建目录确实不存在；再预先创建同名目录并让创建命令复用后触发失败，确认删除任务不会删除该预存目录及其测试文件；同时验证未执行普通任务和已完成任务仍只删记录、运行中链不可选择、清理错误会保留任务。完成后关闭 `wails dev`。
- [ ] 5.5 使用 `scripts/` 中的构建脚本编译可执行程序并打开应用，保持程序运行等待用户确认；确认后把工作树分支合并到当前工作区项目对应分支并解决冲突，再次编译验证合并结果。
- [ ] 5.6 同步最终实现到 OpenSpec 与 `docs/plans/`，归档完成的变更，提交全部 Git 变更，并移除已合并的工作树。
