## 1. 备注模板设置

- [x] 1.1 为默认备注模板、旧设置迁移和显式空模板添加 Go 测试。
- [x] 1.2 在设置模型、存储迁移和 Wails 绑定中持久化备注模板。
- [x] 1.3 在前端类型和设置保存 payload 中映射备注模板。

## 2. 运行期备注模型

- [x] 2.1 添加按终端会话隔离备注与汇总格式化函数的前端测试。
- [x] 2.2 实现自动换行的纯格式化函数和会话记录清理辅助函数。

## 3. 终端选区与发送界面

- [x] 3.1 为读取活动 xterm 选区和关闭会话返回空文本添加测试与注册表方法。
- [x] 3.2 为选区浮动入口、输入期间保留原文的备注框和禁用鼠标处理边界添加组件测试。
- [x] 3.3 实现选区附近的备注按钮、输入对话框、标题栏数量入口和备注面板。
- [x] 3.4 实现按模板模拟粘贴回同一终端，并在成功或失败后清空记录。

## 4. 应用状态与设置界面

- [x] 4.1 为会话隔离、终端退出、关闭、任务结束、任务删除和卸载清理添加 App 测试。
- [x] 4.2 在 App 中维护按终端会话绑定的运行期备注，并传给当前终端视图。
- [x] 4.3 在设置草稿中增加“终端备注”分类和三个模板输入控件。
- [x] 4.4 生成 Wails 绑定并验证设置保存与取消行为。

## 5. 验证与交付

- [x] 5.1 运行受影响 Go 与前端测试、前端构建和 `go test -race ./...`。
- [ ] 5.2 使用持续运行的 `wails dev` 和浏览器工具验证普通终端与禁用鼠标剪贴板终端的完整流程。
- [ ] 5.3 用项目脚本编译并打开应用，等待用户确认后完成分支整合、OpenSpec 归档、提交和 worktree 清理。
