## 1. 隔离开发环境

- [x] 1.1 在当前任务目录创建 `.worktrees`，从当前工作区对应分支创建 `add-gitlab-project-batch-import` 功能分支和独立 worktree，并确认现有未提交变更未被覆盖或带入功能分支。
- [x] 1.2 在 worktree 中记录后端与前端基线测试结果，确认后续失败可归因于本变更。

## 2. GitLab API 客户端

- [x] 2.1 使用 `httptest` 为 GitLab 地址校验、私有部署子路径、HTTP 明文警告、`PRIVATE-TOKEN` 请求头、`/api/v4/user` 用户名匹配与不匹配编写失败测试。
- [x] 2.2 使用 `httptest` 为同主机重定向、跨主机重定向拒绝、`/api/v4/projects` 全量分页、禁止附加 `membership=true`、后续分页失败时不返回部分结果编写失败测试。
- [x] 2.3 为无效地址、超时、证书错误、401、403、404、429、分页异常和令牌脱敏错误信息编写失败测试。
- [x] 2.4 使用 Go 标准库实现 GitLab 客户端、身份校验、全量项目分页、重定向限制、超时与可重试错误映射，使客户端测试通过。
- [x] 2.5 为 SSH、HTTPS、SCP 风格仓库地址的主机与完整项目路径归一化编写失败测试，覆盖 `.git` 后缀、大小写、同主机跨协议重复和不同主机同路径非重复。
- [x] 2.6 实现仓库身份归一化与比较逻辑，并运行 `go test ./internal/gitlab/...` 验证客户端和去重测试。
- [x] 2.7 根据合并前审查补充空 hostname、HTTPS 降级重定向、分页跳页和顶层 `null` 的失败测试并修复，保证令牌与分页完整性。

## 3. 原子批量保存

- [x] 3.1 为批量导入存储层编写失败测试，覆盖一次读取最新数据、沿用现有 Git 默认值与内置模板、项目简单名称、全局 SSH/HTTPS 地址模式和导入结果计数。
- [x] 3.2 为已存在项目禁用、保存时新增重复跳过、跨协议重复、不同主机非重复、无效输入整批回滚和受控保存失败整批回滚编写失败测试。
- [x] 3.3 在单次数据变更锁内实现批量校验、重复复查和一次持久化，确保非重复项目要么全部写入，要么全部不写入。
- [x] 3.4 运行批量存储层的聚焦测试，确认成功数、跳过数、失败回滚和现有单条 Git 信息行为均符合规范。
- [x] 3.5 补充相对 URL 部署与 HTTP/SSH 独立端口测试，改为以 GitLab 返回的两个 clone endpoint 集合识别跨协议重复。
- [x] 3.6 先为专用 GitLab 默认连接字段编写存储层失败测试，再实现明文读取、成功覆盖、保存失败保留旧值以及与其他变更共用原子锁和文件替换。

## 4. 应用接口与 Wails 绑定

- [x] 4.1 为应用层“读取 GitLab 可见项目”和“导入所选项目”接口编写失败测试，覆盖请求参数、凭据不回传、错误脱敏、URL 模式和导入结果。
- [x] 4.2 在 `App` 中实现 GitLab 项目读取与批量导入方法，并定义前端所需的项目、连接错误和导入结果数据结构。
- [x] 4.3 运行应用层聚焦测试，确认 Personal Access Token 只写入专用明文默认连接字段，不进入日志、项目列表返回值或其他持久化模型。
- [x] 4.4 执行 `wails generate module` 重新生成绑定，检查生成的 TypeScript 方法与类型只包含预期变化。
- [x] 4.5 先为默认连接读取与显式保存编写应用层失败测试，再实现 `GetGitLabImportDefaults` 和不回传令牌的 `SaveGitLabImportDefaults`；项目读取不得隐式保存，认证、分页、已有信息读取或持久化失败不得覆盖旧值或泄露令牌。
- [x] 4.6 根据代码审查拆分项目读取与默认连接保存，确保只有前端确认仍有效的当前响应才触发保存，并增加旧请求晚于新请求返回时不得覆盖默认值的回归测试。

## 5. 前端导入交互

- [x] 5.1 为前端 API 包装和类型转换编写失败测试，覆盖连接参数、项目列表、选择 ID、URL 模式、导入计数与结构化错误。
- [x] 5.2 为 Git 信息管理器中的 GitLab 批量导入入口、地址/用户名/令牌表单、HTTP 警告、默认零选择和关闭时清空项目临时状态编写失败测试。
- [x] 5.3 为项目名称与 `namespace/project` 的不区分大小写模糊筛选编写失败测试，覆盖“全选当前结果”“取消选择当前结果”只影响筛选结果，以及切换筛选后保留其他选择。
- [x] 5.4 为归档标签与默认不选、显式全选可选归档项目、已导入项目禁用且不计入可选范围、全局 SSH 默认模式和 HTTPS 切换编写失败测试。
- [x] 5.5 为连接错误保留表单、成功后显示导入/跳过计数、关闭时清空项目临时状态、刷新 Git 列表并展开 Git 分类编写失败测试。
- [x] 5.6 实现 GitLab 批量导入弹窗、连接状态、客户端筛选、基于项目 ID 的选择集合、当前结果批量操作、归档/重复状态和地址模式预览，使交互测试通过。
- [x] 5.7 为 Git 信息管理器和任务 Git 选择器的辅助完整路径显示编写失败测试，覆盖同名项目可区分、无法解析地址时回退且克隆目录仍使用简单项目名称。
- [x] 5.8 实现从仓库地址派生 `namespace/project` 的辅助显示，不新增持久化字段且不改变现有克隆目标目录行为。
- [x] 5.9 运行相关前端测试并修复回归，确认手动添加 GitHub/GitLab 信息的既有流程保持可用。
- [x] 5.10 补充晚到项目响应和导入期间关闭的竞态测试，读取关闭后忽略过期响应，写入期间禁止关闭，并保留刷新失败提示。
- [x] 5.11 先为弹窗打开时自动回填、明文保存提示、关闭后重新读取、失败连接不改变默认值和地址格式恢复 SSH 编写前端失败测试，再实现专用 API 与弹窗初始化流程。
- [x] 5.12 根据代码审查增加读取阶段输入冻结、保存默认连接阶段禁止关闭、导入阶段全部相关控件锁定及导入失败完整恢复状态的回归测试和实现。

## 6. 可控 GitLab 集成环境

- [x] 6.1 增加可本地启动的 GitLab HTTP 模拟服务及测试：校验 `integration-user`/`integration-token`，实现 `/api/v4/user`，并用 `X-Next-Page` 返回至少两页项目。
- [x] 6.2 在模拟数据中包含 `team-a/api`、`team-b/api`、已存在候选项目、归档项目和同名跨命名空间项目，并提供第二页失败开关以验证禁止部分结果。
- [x] 6.3 编写模拟服务使用说明和固定启动命令，例如 `go run ./scripts/gitlab-mock-server --listen 127.0.0.1:18080`，令牌仅用于本地测试。

## 7. Wails 浏览器集成测试

- [x] 7.1 启动本地 GitLab 模拟服务，再以 `wails dev` 启动应用；保留终端颜色且不设置自动退出，根据输出记录调试地址。
- [x] 7.2 使用 `mcp chrome-devtools` 打开调试地址，在 Git 信息管理器中准备一个已存在项目并打开 GitLab 导入弹窗，输入 `http://127.0.0.1:18080`、`integration-user` 和 `integration-token`。
- [x] 7.3 验证明文 HTTP 警告、跨两页加载全部项目、初始无选择、已存在项目禁用、归档标签以及同名跨命名空间项目均正确展示。
- [x] 7.4 筛选 `TEAM-A` 并全选当前结果，再筛选 `team-b`；验证前一筛选选择保留，随后分别执行全选和取消当前结果，确认只改变当前筛选范围及统计数量。
- [x] 7.5 清空筛选，显式选择归档项目，切换 SSH/HTTPS 并核对全部预览地址，最后以 SSH 模式导入所选项目。
- [x] 7.6 验证成功与跳过数量、Git 分类自动刷新并展开、完整项目路径辅助信息和任务选择器可区分两个 `api`，同时确认克隆名称仍为 `api`。
- [x] 7.7 重新打开弹窗，确认新导入项目均禁用；启用模拟服务第二页失败模式后重新连接，确认显示可重试错误且不展示任何部分列表。
- [x] 7.8 完成浏览器测试后关闭 `wails dev` 和 GitLab 模拟服务，并记录调试地址、测试数据、操作步骤与结果供归档复核。
- [x] 7.9 使用隔离配置重新执行浏览器集成测试：成功连接后关闭并重开、重启应用后重开均回填三项明文默认值；失败连接保留当前输入但不覆盖已保存值，成功导入后默认值仍存在。
- [x] 7.10 在读取与保存拆分后重新执行隔离浏览器集成测试：通过浏览器临时包装 Wails 绑定并延迟一次 `ListGitLabProjects`，确认读取时三项输入禁用、取消可用且关闭后的晚到结果不调用保存；延迟一次 `SaveGitLabImportDefaults`，确认“正在保存连接”期间取消和 Escape 均无法关闭；延迟并拒绝一次 `ImportGitLabProjects`，确认连接、读取、筛选、地址格式、复选框、全选和取消全选全部禁用，失败后原状态恢复；最后恢复真实绑定完成导入，重启同一隔离配置并确认默认值仍存在。测试后关闭调试页、`wails dev` 和模拟服务。

## 8. 全量验证与交付

- [x] 8.1 运行 `go test -race ./...`，解决本变更引起的后端失败。
- [x] 8.2 运行 `cd frontend && npm test && npm run build`，解决本变更引起的前端测试或构建失败。
- [x] 8.3 再次运行 `wails generate module` 并检查生成绑定无遗漏或非预期差异。
- [x] 8.4 将当前工作区对应分支合并到功能 worktree 分支，解决冲突后重复聚焦测试、全量测试和前端构建。
- [x] 8.5 执行 `./scripts/build-linux.sh amd64` 编译可执行程序，启动 `build/bin/taskai` 且不自动退出，等待用户确认功能与界面。
- [ ] 8.6 用户确认后，将功能 worktree 分支合并回当前工作区对应分支，并再次执行全量测试、前端构建和 `./scripts/build-linux.sh amd64` 验证合并结果。
- [ ] 8.7 同步相关项目文档和 OpenSpec 实施记录，严格校验后归档 `add-gitlab-project-batch-import` 变更。
- [ ] 8.8 提交全部已确认的 Git 变更，确认工作区干净后移除已合并的 worktree 和功能分支。
