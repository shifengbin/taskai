## 1. 固定命令与数据归一化

- [x] 1.1 在生命周期命令模型和默认设置中新增“克隆指定 Git 仓库”的固定 ID、独立类型、只读说明和 `beforeStart`/`postStart` 适用范围，并更新绑定类型与默认设置测试。
- [x] 1.2 为新命令实现独立的链级参数规范化与解析，要求唯一 `repository=<仓库地址>`、可选唯一安全 `dir=<相对目录>`，并覆盖空值、未知键、重复键、绝对路径和目录越界。
- [x] 1.3 更新内置 Git 模板归一化，使 `branch` 始终为可选文本参数，并覆盖历史必填配置迁移、模板保护和已有任务快照不变的测试。

## 2. 生命周期执行与分支继承

- [x] 2.1 在生命周期调度时解析当前任务模板的字符串 `branch` 并冻结到新命令执行请求；覆盖指定分支、缺失或空分支使用远程默认分支，以及非字符串 `branch` 的错误。
- [x] 2.2 在命令链运行器中实现指定仓库的参数化 Git 直接克隆：不读取 Git 附加信息、不经过 Shell、直接使用任务目录或 `dir` 目标，并复用既有远程分支检查和本地分支回退策略。
- [x] 2.3 实现目标目录安全检查，覆盖工作目录不存在、路径逃逸、符号链接、非目录和非空目录失败，以及安全创建空目标目录、成功后保留 `.git`。
- [x] 2.4 覆盖 `beforeStart` 失败阻止启动、`postStart` 失败保留执行中状态并支持重试、标准输入原样传递，以及与既有批量 Git 克隆命令顺序组合的应用层测试。
- [x] 2.5 调整任务创建和更新服务的保存顺序：先解析模板字段，再向空 Git 附加信息分支回填非空模板分支；覆盖显式分支优先、空模板分支保持为空和模板后续变更不改写快照。

## 3. 界面与文档

- [x] 3.1 更新生命周期编排界面的系统命令说明和链级参数交互，明确 `repository` 必填、`dir` 可选、直接克隆目标、仅两个适用钩子及非空目录失败；补充前端交互测试。
- [x] 3.2 更新 README 的生命周期命令和 Git 克隆章节，区分初始化任务目录的新命令与基于 Git 附加信息的既有批量克隆命令，并说明模板分支和远程默认分支语义。

## 4. 验证

- [x] 4.1 运行相关 Go 单元测试及 `go test -race ./...`。
- [x] 4.2 在 `frontend` 目录运行 `npm test` 和 `npm run build`。
- [x] 4.3 运行 `./scripts/build-linux.sh` 完成项目 Linux 构建验证。
- [x] 4.4 运行 `openspec validate add-task-template-git-clone-command --strict`、`openspec status --change add-task-template-git-clone-command` 和 `git diff --check`。
