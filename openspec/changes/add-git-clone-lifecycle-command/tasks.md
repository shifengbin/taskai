## 1. 命令链数据模型与迁移

- [ ] 1.1 将命令链的有序命令 ID 改为带 `commandId` 和 `arguments` 的引用，并更新规范化、适用范围校验、复制及删除引用检查。
- [ ] 1.2 兼容加载旧 `commandIds` 数据，按原顺序迁移为空参数引用并覆盖持久化迁移测试。
- [ ] 1.3 新增 `git-clone` 系统命令、固定 ID、三个适用钩子及只读 `dir` 参数说明，并保持系统命令不可编辑、不可删除。
- [ ] 1.4 实现链级参数格式校验和有效参数合并，要求 Git 命令引用恰有一个有效的 `dir=<相对目录>` 参数。

## 2. Git 生命周期命令执行

- [ ] 2.1 扩展命令链运行器，使自定义和系统命令均接收合并后的参数，并保留既有标准输入输出传递语义。
- [ ] 2.2 实现可注入的 Git 子进程执行接口及错误诊断，避免通过 Shell 解释仓库、分支或目录参数。
- [ ] 2.3 实现内置 Git 信息快照筛选、字段读取、相对目录解析、项目名称安全校验和已存在目标跳过。
- [ ] 2.4 实现多仓库顺序克隆、远程分支检查、存在分支克隆、缺失分支从远程默认分支创建本地分支及失败后重试行为。
- [ ] 2.5 在应用生命周期调用中传递当前任务和链级参数，确保 Git 命令可用于 `beforeStart`、`beforeEnd` 与 `updateTask`，但不改变默认链选择。

## 3. 命令链配置界面与绑定

- [ ] 3.1 更新 Go 应用契约、Wails 绑定生成文件、前端类型和 API 封装，以传输命令引用、链级参数和系统参数说明。
- [ ] 3.2 在命令库的系统命令概要之后显示只读使用文档，并保留现有系统命令的只读行为。
- [ ] 3.3 在命令链编辑器中为每个选中命令配置链级参数，显示 Git 的 `dir` 用法，并在复制、排序和保存时保留参数。

## 4. 测试、文档与构建验证

- [ ] 4.1 补充设置、存储和命令链执行器测试，覆盖参数合并、旧链迁移、Git 参数校验、系统命令保护和 Git 标准输入输出透传。
- [ ] 4.2 补充 Git 批量克隆测试，覆盖无 Git 信息、多仓库、目标已存在、远程分支存在/缺失、目录或 Git 失败以及重试跳过成功项目。
- [ ] 4.3 补充应用生命周期和前端测试，覆盖三个适用钩子、命令文档、链级参数编辑、复制和 Wails 数据转换。
- [ ] 4.4 更新中文 README，说明 Git 系统命令、`dir` 参数、目录规则、分支回退和推荐的 `beforeStart` 命令顺序。
- [ ] 4.5 运行相关 Go 与前端测试，并使用 `scripts` 中的目标平台构建脚本完成构建验证。
