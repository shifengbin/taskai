## 1. 终端环境行为

- [x] 1.1 调整终端环境构造：仅当 `statusHTTP.APIURL()` 非空时向新终端追加 `TASKAI_STATUS_API`，保留任务 ID、终端 ID 和既有变量值格式。
- [x] 1.2 为标题变化状态管理下独立启用本机 HTTP 服务补充应用层测试，验证新终端获得当前 API 基址。
- [x] 1.3 保留并扩展 HTTP 状态管理、服务未监听及设置切换后的回归测试，验证已创建终端不被回写且之后新建终端按监听状态获得变量。
- [x] 1.4 覆盖普通终端、显示终端的自定义命令和菜单显示终端命令均使用统一环境构造；确认无终端命令、前后置脚本及生命周期自定义 Shell 命令不新增该变量。

## 2. 用户说明

- [x] 2.1 更新设置页 HTTP 帮助和任务详情的系统环境变量说明，将注入条件表述为本机 HTTP 服务正在监听且仅作用于之后新建的终端。
- [x] 2.2 更新命令链扩展规则帮助与 README，说明生命周期首段继续通过 stdin JSON 提供 `baseURL`，不把它表述为终端环境变量。
- [x] 2.3 更新前端组件测试，断言独立 HTTP 服务、HTTP 状态管理和环境变量范围的说明均正确显示。

## 3. 验证

- [x] 3.1 运行 `gofmt`，并执行相关 Go 应用层与终端环境测试。
- [x] 3.2 执行前端 Vitest 测试与生产构建，确认说明文案和绑定类型兼容。
- [x] 3.3 执行 `go test ./...`，记录并处理本变更引入的失败；确认不影响生命周期命令输入和既有 HTTP 服务行为。
