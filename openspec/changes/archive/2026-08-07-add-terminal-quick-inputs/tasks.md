## 1. 快捷输入数据模型与持久化

- [x] 1.1 新增 `internal/quickinput` 模型、稳定 ID 生成、名称与内容校验，以及覆盖空值、超长名称、同名项和多行内容的单元测试。
- [x] 1.2 在 `storage.Data`、默认数据、加载归一化和保存路径中加入快捷输入数组，兼容缺失字段，并为旧数据迁移和完整内容保留添加仓库测试。
- [x] 1.3 在仓库中实现列出、按稳定 ID 保存、删除和完整 ID 序列重排操作；拒绝重复、缺失或外来排序 ID，并为即时持久化和排序写入测试。
- [x] 1.4 在 `App` 与应用契约中暴露快捷输入的列出、保存、删除和重排方法，保留现有错误边界，并补充应用层测试。
- [x] 1.5 重新生成 Wails 前端绑定，检查生成的接口与快捷输入 Go 类型一致。

## 2. 信息管理中的快捷输入管理

- [x] 2.1 在前端类型和 API 封装中接入快捷输入接口，并在应用启动和信息管理窗口打开时加载最新快捷输入列表。
- [x] 2.2 将顶部“额外信息管理”入口和管理窗口标题改为“信息管理”，使用标签页保留默认打开的“额外信息管理”内容，并增加“快捷输入管理”标签。
- [x] 2.3 实现快捷输入列表与新增、编辑弹窗：名称与等宽多行内容输入、名称 100 字限制、内容非空校验、字符数显示、同名内容摘要和名称/内容搜索。
- [x] 2.4 实现新增、编辑、删除确认和手动排序的即时保存；未搜索时按持久化顺序展示，排序失败时恢复已保存顺序并显示错误。
- [x] 2.5 为信息管理标签默认值、编辑校验、同名项、内容搜索、删除确认、排序持久化和空列表补充 React 测试。

## 3. 终端快捷输入选择器

- [x] 3.1 扩展 `TerminalSessionRegistry`，为活动会话增加基于 xterm `paste()` 的快捷输入写入方法；不得追加执行字符，并为关闭会话和多行内容补充测试。
- [x] 3.2 在 `TerminalView` 工具栏增加带平台快捷键提示、无障碍标签和稳定尺寸的快捷输入图标按钮，仅对活动终端显示。
- [x] 3.3 实现锚定终端工具栏的快捷输入选择器：自动聚焦搜索、名称和内容搜索、内容摘要、按已保存顺序的结果、点击和 Enter 插入、方向键选择与 Escape 关闭。
- [x] 3.4 实现仅在终端聚焦时响应 `Ctrl+Shift+P` / `Command+Shift+P` 的键盘入口；插入成功后关闭选择器并恢复原终端焦点，避免把快捷键或内容写入其他终端。
- [x] 3.5 为终端工具栏入口、快捷键提示、搜索、键盘导航、模拟粘贴、无额外 Enter、关闭目标终端和焦点恢复补充前端测试。

## 4. 规格与交付验证

- [x] 4.1 核对实现与 `terminal-quick-inputs` 规格以及 `task-extra-info` 规格增量一致，确认没有加入任务专属内容、变量、分类、同步、导入导出或机密存储承诺。
- [x] 4.2 运行受影响 Go 包测试和 `go test -race ./...`，确认持久化、Wails 接口和现有任务行为未回归。
- [x] 4.3 运行受影响的前端测试、`cd frontend && npm test` 和 `cd frontend && npm run build`。
- [ ] 4.4 使用 `scripts/` 中的项目编译脚本完成构建，并手动验证多行内容在启用 bracketed paste 的 Shell 中通过快捷输入插入后不由 TaskAI 额外提交。
