## 1. 菜单定位

- [x] 1.1 在 `frontend/src/components/TaskTree.tsx` 中实现供按钮下拉和右键菜单共用的视口内位置计算，支持下方优先、向上翻转和水平边界调整。
- [x] 1.2 在菜单实际尺寸可用后应用最终位置；窗口尺寸或锚点滚动位置变化时重新计算，避免菜单脱离触发任务。
- [x] 1.3 为高度超过单侧可用空间的菜单设置最大高度和内部纵向滚动，并在首次定位完成前避免显示错误位置。

## 2. 行为验证

- [x] 2.1 在 `frontend/src/components/TaskTree.test.tsx` 中覆盖列表底部任务通过“任务操作”按钮打开菜单时的向上翻转和完整可访问性。
- [x] 2.2 在同一测试文件中覆盖列表底部和视口右边缘右键打开菜单时的位置边界，以及菜单高度受限时的内部滚动限制。
- [x] 2.3 验证翻转或限高后的菜单仍保留菜单项过滤、生命周期锁定、执行回调、点击外部关闭和 Escape 关闭行为。

## 3. 交付验证

- [x] 3.1 运行 `cd frontend && npm test -- --run src/components/TaskTree.test.tsx`。
- [x] 3.2 运行 `cd frontend && npm run build`。
