## 1. 后端终端路径输入

- [x] 1.1 在终端管理器的活动会话中保留创建时的 `ShellPath`，并实现按 POSIX、fish、PowerShell 与 `cmd.exe` Shell 家族格式化拖放路径的纯 Go 帮助函数；未知 Shell 或空路径返回错误。
- [x] 1.2 新增 App/Wails 的终端拖放路径写入入口：验证活动任务和终端，在全部路径格式化成功后将一个不含换行的文本一次写入现有 PTY 会话。
- [x] 1.3 重新生成 Wails 前端绑定，并在 `frontend/src/api.ts` 暴露对应的类型安全调用。

## 2. 原生拖放交互

- [x] 2.1 在 Wails 应用配置中启用原生文件拖放且不禁用 WebView 的拖放接收链，同时保持非终端区域不作为路径输入目标。
- [x] 2.2 在 `TerminalView` 为当前可见终端内容区域注册 Wails drop target 和文件投放回调，将投放路径及终端标识交给后端写入入口，并在终端切换或卸载时清理监听。
- [x] 2.3 处理 Wails 运行时不可用、拖放路径为空、后端拒绝写入和终端已关闭的情况；非终端区域始终阻止文件拖放的 WebView 默认打开行为，确保不会向 xterm 或 PTY 注入原样或部分路径。
- [x] 2.4 修补 Wails 2.12 Linux 的原生文件投放结束流程：保留 Wails 的路径收集链，在转发完成后显式结束事件，使 WebKit 不会打开投放文件；补丁必须由 Linux 构建脚本在隔离副本中应用。

## 3. 测试与验证

- [x] 3.1 为路径格式化器添加表驱动测试，覆盖每个受支持 Shell 的空格、单引号、变量/通配符、命令分隔符和多路径顺序；在 Windows 上增加 `cmd.exe` 真实解析回归测试。
- [x] 3.2 为终端管理器和 App 入口添加测试，覆盖创建时 Shell 固定、未知 Shell、空路径、关闭会话、单次写入及不追加 Enter/CR/LF。
- [x] 3.3 为终端视图和应用根组件添加前端测试，覆盖仅终端内容区注册、正确调用拖放写入 API、监听清理、无活动终端时阻止 WebView 文件导航，以及后端失败不触发本地原始输入。
- [x] 3.4 运行 `go test -race ./...`、`wails generate module`、`cd frontend && npm test` 与 `cd frontend && npm run build`，并检查生成绑定和 OpenSpec 差异。
- [x] 3.5 为 Wails Linux 拖放补丁增加可重复应用的回归测试，并构建 Linux 产物进行实际桌面投放验收。
