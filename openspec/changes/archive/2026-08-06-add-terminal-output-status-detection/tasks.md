## 1. 设置与实时状态领域

- [x] 1.1 在 `internal/settings` 和 `internal/realtime` 添加失败测试，覆盖 `output-change` 的校验、旧设置默认值、输出活动的 1.5 秒静默转换、连续输出延长静默期、选中态、方式隔离及失效计时器。
- [x] 1.2 扩展状态管理设置和实时状态服务，支持输出方式、基于最后活动时间的单计时器静默判定，以及重复工作中状态的投影去重。

## 2. 应用层与绑定

- [x] 2.1 在 `app_test.go` 添加失败测试，验证非空终端输出只在输出方式下驱动活动，标题和 HTTP 方式保持既有行为，并覆盖独立 HTTP 服务下的新终端环境变量场景。
- [x] 2.2 在终端事件分发和状态设置映射中接入输出活动，保留终端退出、晚到事件和显式 HTTP 状态更新的现有规则。
- [x] 2.3 重新生成 Wails 前端绑定，并检查设置枚举和导出类型包含新方式。

## 3. 设置界面与说明

- [x] 3.1 在前端设置测试中覆盖“根据终端输出变化”的选择、保存和说明文案，同时保留标题变化和 HTTP 方式的既有交互。
- [x] 3.2 在实时状态设置中加入输出方式，并根据当前方式说明其活动信号和 1.5 秒静默后的状态变化。
- [x] 3.3 更新中文使用说明，明确输出方式将任意非空终端输出（含回显和控制序列）视为活动，以及独立 HTTP 服务的适用范围。

## 4. 验证

- [x] 4.1 运行受影响的 Go 包和前端测试，修复本变更引入的问题。
- [x] 4.2 运行 `go test -race ./...`、`cd frontend && npm test && npm run build`、`./scripts/build-linux.sh amd64`、`openspec validate add-terminal-output-status-detection --strict` 与 `git diff --check`。
