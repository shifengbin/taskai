## 1. 终端会话注册表

- [x] 1.1 新增以前端任务 ID 与终端 ID 为键的终端会话注册表，令其拥有 xterm、`FitAddon`、输入/选择监听器和增量输出写入，并将 xterm `scrollback` 固定为 1000 行。
- [x] 1.2 实现会话的首次可见挂载、已有 xterm 根元素重新挂载、容器尺寸适配、可见行刷新、自动聚焦和 PTY 尺寸回调；视图卸载不得处置会话。
- [x] 1.3 实现单终端、任务级和全局会话处置，以及已关闭键对迟到输出和尺寸更新的隔离。
- [x] 1.4 为会话注册表补充单元测试，覆盖后台增量输出、1000 行滚屏配置、首次挂载、A -> B -> A DOM 复用、监听器处置和迟到事件忽略。

## 2. 应用状态与终端宿主集成

- [x] 2.1 移除 `TerminalRecord`、终端事件 reducer 和创建前事件缓存中对无上限原始输出的依赖，使输出事件直接路由到会话注册表，同时保留标题、退出和实时状态更新。
- [x] 2.2 在终端创建返回前到达输出时按事件终端标识建立会话；创建返回后复用该会话，并保持现有的待处理标题、退出状态和任务结束隔离语义。
- [x] 2.3 将 `TerminalView` 改为会话宿主，复用已有 xterm 而非在终端选择时构造、回放和销毁实例；保留右键粘贴、选区复制、关闭、输入和容器尺寸变化行为。
- [x] 2.4 将关闭终端、自然退出、结束任务和 `App` 清理路径接入会话处置，确保它们不影响其他任务的活动会话。

## 3. 前端回归测试

- [x] 3.1 更新 `state` 与 `App` 测试，验证大量输出不再增长 React 终端记录、创建竞态输出仍显示、标题和实时状态行为不回归。
- [x] 3.2 更新 `TerminalView` 测试，验证 A -> B -> A 不重新构造或回放 A 的历史输出，且每次附着仍适配、刷新并聚焦。
- [x] 3.3 增加终端关闭、退出、任务结束和应用卸载的会话资源清理测试，并验证晚到输出不会重建已释放会话。

## 4. 验证

- [x] 4.1 运行前端定向测试，确认会话复用、输出上限和相关交互测试通过。
- [x] 4.2 运行 `cd frontend && npm run build`，确认 TypeScript 与 Vite 构建通过。
- [x] 4.3 运行 `openspec validate improve-terminal-switch-performance --strict`，确认变更工件符合 schema。
- [x] 4.4 运行 `./scripts/build-linux.sh`，并在 Linux Wails 应用中让多个终端产生超过 1000 行输出后反复切换，确认点击保持流畅、最新 1000 行可滚动且终端交互连续。
