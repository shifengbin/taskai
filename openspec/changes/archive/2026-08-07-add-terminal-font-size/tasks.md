## 1. 设置模型和绑定

- [x] 1.1 在 Go 设置包集中定义终端字号的默认值、最小值和最大值，并在设置模型中增加 `terminalFontSize`。
- [x] 1.2 更新默认值、设置校验和旧 JSON 加载路径，使缺失字号按 13px 处理、越界字号归一化到 10px 至 24px。
- [x] 1.3 为默认字号、旧设置兼容和边界归一化编写 Go 单元测试。
- [x] 1.4 更新前端设置类型并重新生成 Wails 绑定，确认生成模型包含 `terminalFontSize`。

## 2. 设置界面和预览

- [x] 2.1 将已保存字号接入设置草稿和保存路径，确保取消或关闭设置不更新终端会话。
- [x] 2.2 在终端字体设置区域增加 10px 至 24px、步长 1px 的字号滑块，以及恢复默认 13px 的图标操作。
- [x] 2.3 显示当前草稿的像素值和相对于 13px 的四舍五入比例，并使中文、英文和终端字符预览使用草稿字体族与字号。
- [x] 2.4 为字号显示、恢复默认、保存回显和取消不生效编写前端设置测试。

## 3. xterm 会话同步

- [x] 3.1 让 `TerminalSessionRegistry` 在创建 xterm 时读取最新已保存字号，并将其设为 xterm `fontSize`。
- [x] 3.2 增加已有会话的字号批量更新操作，保留会话、滚屏和输入监听器，不销毁 xterm。
- [x] 3.3 保存字号后更新所有会话；当前可见终端立即重新 fit、刷新并同步 PTY 行列，后台终端在下次挂载时使用新字号适配。
- [x] 3.4 为新会话、输出先于视图挂载、当前终端 resize、后台终端延迟适配及滚屏保留编写会话和视图测试。

## 4. 验证

- [x] 4.1 运行相关 Go 测试、重新生成 Wails 绑定并检查生成的前端接口差异。
- [x] 4.2 运行前端类型检查、测试和生产构建。
- [x] 4.3 手动验证设置内预览、保存后当前终端字号和网格变化、切换后台终端后的适配，以及取消设置时终端不变。
