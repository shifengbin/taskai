## 1. 终端画面变化检测

- [x] 1.1 为终端可见画面快照和差分补充前端测试，覆盖普通文字、空格覆盖、空白背景或样式、同字符改色、无差异重写、光标控制和仅设置后续颜色。
- [x] 1.2 在 `TerminalSessionRegistry` 中维护活动缓冲区的画面基线，并在 xterm 解析批次后仅对实际画面变化发出活动回调。
- [x] 1.3 在会话初始化、尺寸适配、外观更新和销毁时正确建立或清理基线，确保界面重新渲染不会被判定为终端活动。

## 2. 实时状态桥接

- [x] 2.1 增加从前端上报终端画面活动的 Wails 应用方法，并让其复用实时状态服务的 `output-change` 模式校验和静默计时。
- [x] 2.2 删除原始非空 PTY 输出的直接活动上报；仅在输出判定方式下从前端发起画面活动调用，并保留设置切换时的后端隔离。
- [x] 2.3 重新生成 Wails 前端绑定，并补充应用层及前端事件处理测试，验证标题方式和 HTTP 方式不会接受画面活动。

## 3. 验证与交付

- [x] 3.1 运行受影响的 Go 包与前端终端会话、应用状态测试，并修复本变更引入的问题。
- [x] 3.2 运行 `go test -race ./...`、前端测试和前端构建；使用 `scripts/` 中的脚本编译并在未禁用终端颜色的环境中启动应用进行人工检查。
- [x] 3.3 同步已确认的实施记录和 OpenSpec 规格；在用户确认后归档变更、提交 Git 变更并移除已合并的工作树。
