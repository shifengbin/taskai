## 1. 开发隔离与检测模型

- [x] 1.1 按项目规范在仓库 `.worktrees` 下创建并验证本变更的独立 Git worktree，记录当前工作区分支与 worktree 分支的合并目标。
- [x] 1.2 在设置包中先添加失败测试，覆盖仅检测到 `codex`、仅检测到 `claude`、同时检测到两者、均未检测到以及单个检测报错不影响另一个命令。
- [x] 1.3 实现可注入的跨平台命令查找与检测结果模型，使用系统命令查找规则识别 `codex`、`claude`，并保证 Windows `PATHEXT` 可解析的 `.exe`、`.cmd` 等形式被视为可用。

## 2. 自动任务菜单合并与持久化

- [x] 2.1 在设置包中先添加失败测试，验证自动项的稳定标识、准确名称、命令、单独参数、显示终端属性和固定追加顺序。
- [x] 2.2 添加幂等合并测试，验证已有稳定标识的菜单项保留内容与位置，相似名称或命令的用户自定义项不被接管，未检测到的命令不新增也不删除已有设置。
- [x] 2.3 实现 Codex 与 Claude 自动菜单定义及追加式合并函数，保持现有系统菜单项、用户自定义项和顺序不变，并仅在结果变化时返回持久化信号。
- [x] 2.4 在应用启动编排中注入命令查找器，读取并合并设置，仅在新增自动项时通过仓储保存完整设置；添加应用层测试验证重复启动不重复写入、单项保存失败不阻止应用初始化且不会产生部分更新。
- [x] 2.5 将启动合并下沉为仓储锁内的原子更新，并为导出设置读写增加启动就绪屏障；测试最新用户设置不被旧快照覆盖且并发调用等待同步完成。

## 3. 跨平台内嵌终端启动

- [x] 3.1 添加 Unix 终端回归测试，验证自动菜单命令经配置 Shell 在任务操作目录启动，并分别保持 `--yolo` 与 `--dangerously-skip-permissions` 的单参数边界。
- [x] 3.2 添加 Windows 构建约束测试，覆盖配置为 `cmd.exe`、PowerShell 时启动 `.cmd` 包装命令，以及普通 `.exe` 命令的参数构造和 ConPTY 启动行为。
- [x] 3.3 调整 Windows 带命令终端的启动路径，使其复用配置 Shell 的命令执行语义并支持 `.cmd` 包装文件，同时保持普通 Shell 终端、工作目录、环境变量、退出状态和无额外后台黑框行为不变。
- [x] 3.4 让 Unix 内嵌终端复用共享命令构造器，增加 fish 参数协议回归测试，并在原生 Windows 测试中覆盖 `.cmd` 经 ConPTY 启动、参数、工作目录和退出状态。

## 4. 设置界面与执行回归

- [x] 4.1 添加前端回归测试，验证后端返回的 `codex`、`claude` 自动项出现在菜单设置、任务右键菜单和“任务操作”下拉菜单，顺序与保存结果一致。
- [x] 4.2 添加应用执行测试，验证选择两个自动项时调用现有显示终端命令路径，并将准确的命令、参数和当前任务操作目录传入终端管理器。
- [x] 4.3 若导出的 Wails 方法或 Go 类型发生变化，运行 `wails generate module` 并核对 `frontend/wailsjs/`；若边界未变化，确认无需更新生成绑定。

## 5. 自动验证与集成测试

- [x] 5.0 开发完成后先将当前工作区 `main` 分支合并到 worktree 分支，解决冲突并确认待验证代码包含最新工作区变更。
- [x] 5.1 运行受影响的 Go 与前端测试，至少覆盖设置合并、启动持久化、Unix/Windows 终端命令构造、任务菜单设置和任务树执行场景；修复失败后重复运行直至通过。
- [x] 5.2 运行 `go test -race ./...`、`cd frontend && npm test && npm run build`，并执行 Windows 目标交叉编译或 Windows 专用测试编译，确认所有平台构建约束有效。
- [x] 5.3 准备测试专用 `PATH` 中可观察参数与工作目录的 `codex`、`claude` 替身命令，使用 `wails dev` 启动应用且不设置 `NO_COLOR` 等颜色禁用变量，从输出取得调试地址并保持应用运行。
- [x] 5.4 使用 MCP Chrome DevTools 打开调试地址，进入菜单设置确认依次出现 `codex` 与 `claude` 且均标记“显示终端”；创建并启动带实际操作目录的测试任务，分别从右键菜单和“任务操作”下拉菜单启动两个命令，核对内嵌终端记录到的命令、单独参数和工作目录。
- [x] 5.5 保持测试设置不变重启一次 `wails dev`，使用 MCP Chrome DevTools 确认两个自动项均只有一个、顺序未变化且用户已有菜单项未被修改；再分别从测试 `PATH` 移除一个和两个替身命令重启，确认未检测到的命令不新增且已有设置不被删除，测试完成后关闭调试应用。
- [x] 5.6 运行 `openspec validate add-detected-agent-task-menus --strict`，使用 `scripts` 下对应平台构建脚本编译可执行程序并打开程序，保持程序运行等待用户确认。

## 6. 合并、文档与归档

- [x] 6.1 用户确认后把已验证的 worktree 分支合并回当前工作区 `main` 分支，并解决可能出现的冲突。
- [x] 6.2 合并后重新运行项目编译和关键自动测试，确认合并结果与开发 worktree 一致。
- [x] 6.3 同步正式规格和中文实施记录，并确认 OpenSpec 归档所需的制品与任务全部完成。

## 7. 删除记忆与已有命令去重

- [x] 7.1 先添加设置层失败测试，覆盖已有 `command` 精确为 `codex` 或 `claude` 时不追加、名称相同但命令不同不误判，以及删除记录抑制自动项。
- [x] 7.2 实现稳定 ID、删除记录和精确命令字段共同参与的启动合并规则，并通过设置层定向测试。
- [x] 7.3 先添加仓储失败测试，覆盖删除自动项持久化、重载后仍抑制、删除自定义项不记录，以及设置保存失败不留下删除记录。
- [x] 7.4 在仓储根数据增加隐藏删除记录，保存设置时原子记录自动稳定 ID 的删除，并让启动合并读取该记录。
- [x] 7.5 添加前端删除自动项保存回归测试，确认保存请求只移除目标自动项并保留其他菜单。
- [x] 7.6 重跑完整自动测试、Windows 交叉编译、OpenSpec 严格校验和 `wails dev` 浏览器重启集成测试；重新构建并打开程序等待确认。
