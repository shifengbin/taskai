## 1. 修复管理页折叠与滚动布局

- [x] 1.1 移除额外信息管理“分类模板”内容的 `forceMount`，保持 `templateSectionExpanded` 受控状态和信息分组状态不变。
- [x] 1.2 将额外信息管理主体改为同时设置最大高度和纵向自动滚动的容器，保留标题、信息内容、搜索行为及弹窗底部“关闭”操作。
- [x] 1.3 检查该管理页调整后的窄宽度换行、模板操作按钮和滚动条隐藏样式，确保模板行不会覆盖信息区域。

## 2. 补充前端回归测试

- [x] 2.1 更新分类模板折叠测试，验证第二次点击后模板行不可见或已卸载，并确认下方信息分组仍存在且状态未被重置。
- [x] 2.2 增加管理主体滚动布局测试，验证其具有最大高度和 `overflow-y: auto`，并确认“关闭”按钮仍位于额外信息管理弹窗中。
- [x] 2.3 使用多条模板和信息数据覆盖滚动内容顺序、搜索筛选及折叠后信息区域可访问，避免只断言 `aria-expanded`。

## 3. 验证与交付

- [x] 3.1 运行额外信息管理相关的 `App.test.tsx` 测试，并根据失败结果修正实现或断言。
- [x] 3.2 运行 `cd frontend && npm test` 与 `cd frontend && npm run build`，确认前端测试和构建均通过。
- [x] 3.3 检查 `git diff` 与 OpenSpec 状态，确认没有修改后端接口、持久化数据或无关文件。
