## 1. 终端名称数据与派生逻辑

- [x] 1.1 在前端终端会话记录中增加仅运行期使用的可选别名，并实现实际名称、展示名称和两行提示内容的共享派生逻辑。
- [x] 1.2 为别名优先显示、空白别名归一化为清除、实际名称回退和缺失启动命令的提示内容补充单元测试。

## 2. 双入口别名编辑与提示

- [x] 2.1 在任务树终端项中接入双击就地编辑、Enter/失焦保存、Escape 取消，以及仅有别名时的两行悬浮提示。
- [x] 2.2 在右侧终端标题栏中接入相同的双击编辑和两行悬浮提示，并确保两处由同一终端会话状态同步更新。
- [x] 2.3 为任务树和终端标题栏补充组件测试，覆盖设置、修改、清空和取消别名，以及提示的两行实际信息。

## 3. 状态隔离与验证

- [x] 3.1 确保别名更新只改变展示字段，实际 OSC 标题解析和 `title-change` 活动上报继续只读取实际标题。
- [x] 3.2 补充状态管理回归测试，验证设置、修改和清空别名不改变实时状态，而实际标题变化在存在别名时仍可上报活动。
- [x] 3.3 运行受影响的前端测试，并执行 `npm test` 和 `npm run build` 验证类型、组件和打包结果。

## 4. 悬浮提示标签调整

- [x] 4.1 将别名悬浮提示调整为固定的 `标题: <实际标题>` 和 `命令: <启动命令>` 两行，并更新相关组件测试。
