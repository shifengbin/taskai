## 1. 隔离开发环境

- [x] 1.1 在任务目录创建 `.worktrees`，为本变更创建独立 worktree 分支，并确认不带入当前工作区的无关变更
- [x] 1.2 检查 worktree 中 `TerminalName`、任务树调用点和现有提示测试，确认实现基线与设计一致

## 2. 测试驱动实现

- [x] 2.1 先为 `TerminalName` 增加失败测试，覆盖可选的右侧定位、顶部对齐、8px 间距和提示不接收鼠标事件
- [x] 2.2 先为任务树增加失败测试，确认任务树调用启用固定右侧提示策略，右侧终端标题栏继续保持默认策略
- [x] 2.3 为 `TerminalName` 增加可选提示定位参数并传递给现有 `TooltipContent`，同时禁用终端别名提示的鼠标事件
- [x] 2.4 在任务树终端名称调用点传入右侧、顶部对齐、8px 间距并关闭该实例的碰撞翻转，不修改右侧终端标题栏调用方式
- [x] 2.5 运行 `TerminalName`、`TaskTree` 和 `TerminalView` 相关前端测试，确认新增及既有行为全部通过

## 3. 自动验证与桌面集成测试

- [x] 3.1 运行完整前端测试与生产构建，并运行 `go test -race ./...` 检查跨层回归
- [x] 3.2 使用 `wails dev` 保持应用运行，通过浏览器工具悬浮多个相邻的任务树终端，确认提示显示在右侧且不会遮挡或阻止终端点击
- [x] 3.3 在同一次集成测试中确认右侧终端标题栏提示仍按既有位置显示，两行内容和别名编辑行为不变
- [x] 3.4 使用 `scripts` 中的构建脚本编译可执行程序并打开程序，重复关键悬浮和点击场景后等待用户确认

## 4. 合并、文档与清理

- [x] 4.1 用户确认后，将当前工作区对应分支合并到 worktree 分支并解决冲突，再将已验证的 worktree 分支合并回当前工作区项目分支
- [x] 4.2 在合并后的项目分支重新编译并运行相关测试，确认合并结果可用
- [x] 4.3 同步最终实现与 OpenSpec/计划文档，校验并归档本变更
- [x] 4.4 提交全部相关 Git 变更，并移除已经合并的 worktree
