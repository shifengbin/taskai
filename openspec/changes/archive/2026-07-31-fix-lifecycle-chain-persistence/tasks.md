## 1. 回归测试与复现

- [x] 1.1 在仓库测试中构造包含自定义命令、命令链和默认映射的设置，验证普通设置保存不会覆盖生命周期配置。
- [x] 1.2 在应用测试中覆盖“专用接口保存命令链 -> 创建任务 -> 开始任务”的完整流程，并断言链仍可解析和执行。
- [x] 1.3 增加缺少生命周期字段、携带过期生命周期字段的旧快照测试，固定兼容和保护语义。

## 2. 后端字段所有权调整

- [x] 2.1 修改仓库普通设置保存逻辑，使其从最新数据保留生命周期命令、命令链和默认链，只写入普通设置字段。
- [x] 2.2 新增仓库层默认链专用读改写方法，并迁移应用层 `SaveLifecycleDefaultChain` 使用该方法。
- [x] 2.3 保留生命周期命令和命令链专用接口的校验、归一化、引用保护及原子保存行为。
- [x] 2.4 更新受影响的 Go 测试辅助方法和调用方，确保生命周期配置通过专用接口建立。

## 3. 前端请求边界

- [x] 3.1 调整普通 `saveSettings` 请求构造，移除生命周期命令、命令链和默认链字段。
- [x] 3.2 保留专用生命周期接口返回结果对设置状态的刷新，避免界面继续使用旧配置快照。
- [x] 3.3 为请求裁剪和配置刷新补充前端类型或 API 单元测试（如现有测试层可覆盖）。

## 4. 验证与文档

- [x] 4.1 运行 `gofmt`、`go test -race ./...`，确认后端回归测试通过。
- [x] 4.2 运行前端测试和生产构建，确认生成的请求类型与 Wails 绑定兼容。
- [x] 4.3 更新 README 或接口说明，记录普通设置与生命周期专用保存接口的字段边界。
