## 1. 状态迁移处理

- [x] 1.1 整理前端任务快照更新入口，在初始加载后的已确认状态迁移中识别“未执行→执行中”和“执行中→已完成”。
- [x] 1.2 保留“未执行→执行中”的标签切换及设置持久化，并使开始响应和生命周期事件共用该行为。
- [x] 1.3 移除结束任务成功后的“已完成”标签切换，确保任务、计数和终端状态仍按既有规则刷新。
- [x] 1.4 与 `improve-lifecycle-command-progress` 的前端事件实现协调，避免异步生命周期事件对“执行中→已完成”重新触发自动切换。

## 2. 任务迁移反馈

- [x] 2.1 在应用层维护包含任务 ID 和递增标识的一次性开始反馈令牌，并在动效结束或超时后清理。
- [x] 2.2 扩展任务树属性和目标任务条目，为正在反馈的执行中任务提供稳定的测试标识。
- [x] 2.3 实现两次快速且不改变布局的任务条目闪烁/高亮，并在减少动态效果偏好下替换为短暂静态高亮。

## 3. 自动化验证

- [x] 3.1 补充应用测试：开始成功后切换并持久化执行中标签，开始失败或仍未进入执行中时不切换。
- [x] 3.2 补充应用测试：生命周期事件将任务变为执行中时同样触发切换和反馈，结束任务后保持原标签且不保存已完成标签。
- [x] 3.3 补充任务树组件测试：仅目标任务带有反馈标识，其他任务不受影响，并验证反馈样式的减少动态效果分支。
- [x] 3.4 运行 `cd frontend && npm test && npm run build`。

## 4. 项目编译

- [x] 4.1 运行 `./scripts/build-linux.sh`，确认项目构建产物生成成功。
