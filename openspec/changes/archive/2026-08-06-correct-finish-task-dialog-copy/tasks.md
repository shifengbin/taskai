## 1. 结束任务确认弹窗

- [x] 1.1 更新 `frontend/src/App.tsx` 的结束任务说明，移除固定删除工作目录的承诺，说明终端关闭和按配置执行的结束后命令链。
- [x] 1.2 将确认按钮文字改为“结束任务”，保留既有确认处理、危险操作样式与取消行为。

## 2. 测试与验证

- [x] 2.1 更新 `frontend/src/App.test.tsx` 的结束任务测试，断言新的说明与按钮文字，并继续覆盖取消不调用接口、确认调用既有结束任务接口和释放终端会话。
- [x] 2.2 运行相关前端测试及前端构建，确认文案变更不影响既有任务结束交互。
