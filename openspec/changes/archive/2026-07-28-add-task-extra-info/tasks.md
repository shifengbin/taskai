## 1. 领域模型与持久化

- [x] 1.1 定义额外信息模板、参数定义、任务附加信息快照和参数值的领域模型，并实现非空、展示名称唯一、参数键唯一及字段冲突校验。
- [x] 1.2 扩展仓储数据结构以独立保存模板集合，并实现模板的列出、创建、更新和删除操作及旧 JSON 缺失字段的空集合兼容。
- [x] 1.3 为模板校验、任务快照校验、模板持久化和旧数据读取补充 Go 单元测试。

## 2. 任务生命周期与 HTTP 详情

- [x] 2.1 扩展创建任务和更新任务应用绑定及生命周期服务，使其接收、校验并持久化附加信息快照。
- [x] 2.2 实现模板管理应用绑定，并保证删除模板不会变更已有任务快照。
- [x] 2.3 扩展任务详情 HTTP 资源映射，以 `catalogue` 聚合快照，并输出信息键、静态值和参数键、参数值的扁平对象；保持任务列表接口不变。
- [x] 2.4 为创建/编辑任务、模板删除后的历史任务、HTTP 聚合、空附加信息和可选参数空值补充应用层与 HTTP 测试。

## 3. 桌面界面与绑定

- [x] 3.1 更新前端类型、Wails 绑定调用和状态管理，以支持模板管理及任务附加信息快照。
- [x] 3.2 在设置按钮旁新增额外信息管理入口和独立管理弹窗，支持模板及多参数的新增、编辑、删除和表单错误展示。
- [x] 3.3 在新建和编辑任务弹窗中按 `catalogue` 分组展示信息展示名称，支持多选、按需显示参数输入、必填校验，以及已删除模板快照的编辑和移除。
- [x] 3.4 为管理弹窗、任务附加信息选择、参数校验和编辑历史快照补充前端测试。

## 4. 文档、验证与构建

- [x] 4.1 更新中文 README 和应用内 HTTP 帮助，说明额外信息管理入口、任务快照语义和任务详情 `extraInfo` 的聚合响应格式。
- [x] 4.2 运行 `go test -race ./...`、`cd frontend && npm test && npm run build` 和 `git diff --check`，修复本变更导致的失败。
- [x] 4.3 使用 `./scripts/build-linux.sh` 构建新的 Linux 可执行程序，并确认产物位于 `build/bin`。

## 5. 分类与多字段扩展

- [x] 5.1 扩展领域模型和仓储，独立持久化分类名称、模板的多个固定字段及快照；实现分类增删、空分类删除校验，并将旧单键值模板和快照兼容为一个固定字段。
- [x] 5.2 扩展应用绑定、生命周期校验和 HTTP 详情映射，使模板只能关联已有分类，且详情按 `catalogue` 聚合并平铺所有固定字段和参数值；补充分类删除、迁移和 HTTP 聚合测试。
- [x] 5.3 更新 Wails 绑定和前端类型，在独立管理界面支持分类的新建、选择和删除、多个固定字段及多个参数的编辑；在任务表单支持分类筛选、跨分类保留选择和可移除的已选方形标签。
- [x] 5.4 补充前端交互、服务端迁移与历史快照回归测试，并更新中文 README、OpenSpec 文档和应用内 HTTP 帮助。
- [x] 5.5 运行 `go test -race ./...`、`cd frontend && npm test && npm run build`、`openspec validate add-task-extra-info --strict`、`git diff --check`，并使用 `./scripts/build-linux.sh` 生成新的 Linux 可执行程序。
