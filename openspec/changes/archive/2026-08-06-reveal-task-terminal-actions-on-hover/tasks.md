## 1. 任务树上下文操作

- [x] 1.1 在 `TaskTree` 为任务行的展开、执行、结束、重试和任务操作按钮建立局部操作容器，并保持状态点、生命周期提示和批量选择复选框始终可见。
- [x] 1.2 为任务树中的终端子项关闭按钮应用同一局部操作可见性机制，不改变终端选择、状态展示或关闭回调。
- [x] 1.3 在任务操作菜单打开期间保持其所属任务行的操作容器显示，并保留菜单的锚点、内容和关闭行为。

## 2. 终端标题栏与样式

- [x] 2.1 在 `TerminalView` 为活动终端的关闭按钮建立局部操作容器，保留标题和实时状态点的现有布局与可见性。
- [x] 2.2 添加作用域化样式：仅在支持悬停的环境默认视觉隐藏上下文操作控件，在容器悬停、焦点进入或任务菜单打开时显示；保留固定布局占位与既有减少动态效果支持。
- [x] 2.3 在不支持悬停的设备上保持上下文操作控件直接可见，并确认全局 `IconButton`、顶栏、详情页和对话框按钮不受影响。

## 3. 测试与验证

- [x] 3.1 更新 `TaskTree` 组件测试，覆盖任务行和终端子项的操作容器范围、任务菜单打开状态，以及既有按钮标签、禁用条件和回调不变。
- [x] 3.2 更新 `TerminalView` 组件测试，覆盖标题栏关闭按钮的局部操作容器以及现有关闭行为不变。
- [x] 3.3 运行受影响的前端测试和 `npm run build`，确认类型检查、样式构建和既有任务/终端交互测试通过。
