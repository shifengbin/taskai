## 1. 字体发现与设置模型

- [x] 1.1 增加跨平台终端字体发现接口与共享的字体候选结果类型，返回字体族名称和 spacing 类别。
- [x] 1.2 实现 Windows、macOS 和 Linux 的系统字体发现，并只保留单宽或双宽候选、去重排序及空结果默认回退。
- [x] 1.3 为字体候选归一化、筛选和默认回退编写单元测试，并在各平台构建目标上验证实现可编译。
- [x] 1.4 在 Go 设置模型、默认值、校验和持久化路径增加可选的 `terminalFontFamily` 字段，确保旧设置文件按空值加载。
- [x] 1.5 向 Wails 暴露只读字体发现 API，并重新生成前端绑定和前端设置类型。

## 2. 设置界面与预览

- [x] 2.1 在设置对话框中加载字体候选，并为加载、发现失败和空结果提供可操作的默认字体状态。
- [x] 2.2 增加可滚动的终端字体选择列表，展示字体族名称、spacing 标签、中文预览、英文预览和终端字符预览。
- [x] 2.3 将选中字体写入设置草稿，保证取消时不修改已持久化配置，保存后重新打开可正确回显。
- [x] 2.4 为已保存但当前不可发现的字体提供回退显示，保证用户仍可保存其他设置。

## 3. 新终端会话接入

- [x] 3.1 在前端集中定义默认等宽字体回退栈和 `terminalFontFamily` 到 xterm CSS `fontFamily` 的解析逻辑。
- [x] 3.2 将字体设置提供给 `TerminalSessionRegistry`，确保 xterm 在任何创建路径中都读取最新已保存的字体。
- [x] 3.3 保持现有 xterm 会话不因设置保存而更新字体、重新 fit 或调整 PTY 行列尺寸。
- [x] 3.4 为设置后新建终端、输出先于视图挂载和已有会话保持不变添加前端测试。

## 4. 验证

- [x] 4.1 运行 Go 测试及 Windows、macOS、Linux 相关构建检查，验证字体发现和设置兼容性。
- [x] 4.2 运行前端类型检查、测试和生产构建，验证字体设置、预览和新会话字体行为。
- [x] 4.3 手动验证保存字体后只有新打开或新创建的终端使用新字体，已有终端的滚屏和 PTY 尺寸保持不变。
