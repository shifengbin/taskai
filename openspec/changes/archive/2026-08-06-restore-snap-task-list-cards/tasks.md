## 1. Snap 样式回归测试

- [x] 1.1 更新阴影令牌测试，锁定 2px、3px、4px 的零模糊硬投影层级。
- [x] 1.2 扩展任务树样式测试，锁定顶层任务项的完整描边、内嵌任务色条、悬停提升和钴蓝选择态。
- [x] 1.3 扩展任务树样式测试，锁定终端子项的次级表面、描边、硬投影、悬停提升和选中态，以及启动反馈不覆盖卡片投影。

## 2. Snap 任务列表实现

- [x] 2.1 在 `frontend/tailwind.config.cjs` 恢复 `shadow-snap-sm`、`shadow-snap`、`shadow-snap-lg` 为 2px、3px、4px 硬投影。
- [x] 2.2 实现顶层任务项的 Snap 卡片外壳、任务色内嵌条、钴蓝选择态与非重排悬停反馈，并保留既有任务色背景、操作和拖拽逻辑。
- [x] 2.3 实现终端子项的 Snap 卡片外壳、选择态与悬停反馈，并将启动反馈迁移为不覆盖常态硬投影的独立视觉层。

## 3. 验证

- [x] 3.1 运行受影响的前端测试和完整前端测试套件。
- [x] 3.2 运行 `npm run build`、OpenSpec 严格校验和 Linux 应用编译，确认样式与类型检查通过。

## 4. 参考色条对齐

- [x] 4.1 更新任务卡片样式回归测试，锁定默认、选中与悬停的 5px 圆角内阴影色条。
- [x] 4.2 将任务色标从独立伪元素替换为与参考一致的圆角内阴影，并保留选中态色条。
- [x] 4.3 运行受影响的前端测试、生产构建和严格规范校验。

## 5. 固定两行任务正文

- [x] 5.1 扩展任务树测试，锁定任务卡片 60px 高度、参考标题/描述字体字号间距与“暂无描述”缺省文案。
- [x] 5.2 实现固定两行任务正文、参考标题/描述排版与统一的悬停提示缺省文案，保留截断和操作区域。
- [x] 5.3 运行受影响的前端测试、完整前端测试、生产构建、严格规范校验和 Linux 打包。
