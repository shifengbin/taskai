## 1. 预置与迁移模型

- [x] 1.1 与 `fix-lifecycle-chain-persistence` 的设置字段所有权实现对齐，确定预置迁移版本的持久化位置和普通设置保存边界。
- [x] 1.2 定义稳定的默认分支模板和两条预置命令链 ID，并在新安装默认设置中写入预置内容与最新迁移版本。
- [x] 1.3 实现旧数据的一次性预置迁移：补齐缺失模板和链、仅在当前模板为空时激活默认分支模板、保留默认链映射；版本 2 将未修改的旧 `iterations-ai` 调整为开始前链，版本 3 补回版本 2 中异常缺失的默认分支模板。
- [x] 1.4 确保迁移版本已更新后，用户编辑或删除预置链不会在后续加载时被归一化逻辑重新创建。

## 2. 生命周期范围与链配置

- [x] 2.1 将“创建任务工作目录”和“Git 仓库克隆”固定命令的适用范围扩展到 `beforeStart` 和 `postStart`，并保持既有创建目录默认链仅适用于 `beforeStart`。
- [x] 2.2 配置 `iterations-ai` 预置链的 `beforeStart` 范围、四条命令顺序及指定仓库和 `dir=workspaces` 参数。
- [x] 2.3 配置“更新仓库”预置链的 `updateTask` 范围、清单生成和 `dir=workspaces` Git 克隆步骤。
- [x] 2.4 保持两个预置链为普通可删除链，并复用已有的任务引用删除保护与链参数校验。

## 3. 交互、文档与测试

- [x] 3.1 更新设置和任务表单的绑定或前端测试，覆盖默认激活的必填“默认分支”字段及用户已有模板选择不被迁移覆盖。
- [x] 3.2 增加 Go 设置与存储测试，覆盖新安装预置、旧设置单次迁移、迁移后删除不复建、默认链不包含新链及创建目录命令范围。
- [x] 3.3 增加应用层生命周期测试，覆盖 `iterations-ai` 的 `beforeStart` 执行顺序与参数，以及“更新仓库”的 `updateTask` 清单后克隆顺序。
- [x] 3.4 更新 README，说明默认分支模板、两条可删除预置链、其不自动选择的行为，以及“更新仓库”链不会执行 Git 拉取。
- [x] 3.5 增加 Git 显式工作目录和开始前链完成事件先于旧开始响应时的回归测试。

## 4. 验证

- [x] 4.1 运行 `gofmt` 并执行 `go test ./...`。
- [x] 4.2 运行前端测试与生产构建，确认 Wails 绑定类型和默认模板表单正常。
- [x] 4.3 执行 `scripts/build-linux.sh`，确认 Linux 打包流程通过。
