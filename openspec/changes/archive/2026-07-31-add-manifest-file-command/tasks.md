## 1. 生命周期命令配置与参数

- [x] 1.1 在生命周期设置中新增“生成清单文件”的固定命令类型、标识、默认定义、中文说明和三个允许钩子。
- [x] 1.2 实现 `dir`、`name` 链级参数的规范化与解析，包含默认值、重复与未知参数、文件名和相对目录安全校验。
- [x] 1.3 补充设置与命令链测试，覆盖固定命令不可修改删除、允许钩子范围及参数校验。

## 2. 清单生成与生命周期调度

- [x] 2.1 引入 YAML 序列化依赖，并定义迭代、描述和仓库条目的结构化清单数据模型。
- [x] 2.2 实现从任务 Git 快照构建清单内容的逻辑，保留选择顺序，处理空仓库列表，并在 Git 分支为空时回填任务模板 `branch`。
- [x] 2.3 实现受任务工作目录约束的输出路径准备与原子文件替换，覆盖子目录创建、符号链接及非普通目标拒绝。
- [x] 2.4 将清单生成接入命令链执行器与应用层模板分支解析，确保系统命令不改变自定义命令的数据管道并遵循既有钩子失败语义。
- [x] 2.5 补充生命周期执行器和应用服务测试，覆盖默认与自定义路径、YAML 特殊字符、分支优先级、空分支、重复覆盖和各类不安全路径。

## 3. 界面说明、文档与回归验证

- [x] 3.1 更新生命周期编排界面及相关前端类型或测试，使新固定命令显示其中文说明和可配置参数。
- [x] 3.2 更新 README 的生命周期命令文档，说明清单字段、参数、分支回填、覆盖行为与 `beforeStart` 的命令排序要求。
- [x] 3.3 运行 `go test -race ./...`、`npm test` 和 `npm run build`，修复本变更引入的失败并记录验证结果。

## 验证记录

- 2026-07-31：`go test -race ./...` 通过。
- 2026-07-31：`npm test -- --run` 通过。
- 2026-07-31：`npm run build` 通过；保留依赖包的 `use client` 提示及既有包体积提示。
- 2026-07-31：`GOOS=windows GOARCH=amd64 go test -c .` 通过，用于校验 Windows 安全写入实现可编译。
