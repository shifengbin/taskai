## 1. Git 克隆参数解析

- [x] 1.1 调整 Git 克隆参数规范化：允许空参数数组并保留为空，显式参数仍仅接受唯一、安全的 `dir=<相对目录>`。
- [x] 1.2 调整 Git 克隆目录解析：空参数返回任务工作目录标识 `.`，显式 `dir=.` 和自定义子目录保持现有结果。
- [x] 1.3 补充设置层测试，覆盖空参数、仅空白行、显式当前目录、自定义目录及全部无效显式参数。

## 2. 生命周期执行回归

- [x] 2.1 补充命令链执行测试，验证未填写 Git 克隆参数时每个项目克隆至 `<任务工作目录>/<项目名称>`。
- [x] 2.2 保留并验证显式 `dir=<相对目录>` 的目标路径、目录越界防护和已存在目标跳过行为。

## 3. 使用说明与前端交互

- [x] 3.1 更新 Git 仓库克隆系统命令说明，明确参数可留空及默认与自定义目录的最终目标路径。
- [x] 3.2 更新命令链编辑器交互测试，验证不填写追加参数可保存，并以空参数数组提交。
- [x] 3.3 用中文更新 README 的 Git 仓库克隆章节，说明默认任务目录与 `dir=<相对目录>` 用法。

## 4. 验证

- [x] 4.1 运行相关 Go 测试，以及 `go test -race ./...`。
- [x] 4.2 在 `frontend` 目录运行 `npm test` 和 `npm run build`。
- [x] 4.3 运行 `./scripts/build-linux.sh` 完成项目 Linux 构建验证。
- [x] 4.4 运行 `openspec validate make-git-clone-directory-optional --strict` 与 `git diff --check`。
