## 1. 失败回归覆盖

- [x] 1.1 在 `App.SaveSettings` 测试中从已保存的任务模板开始，提交不含模板字段的普通设置，并断言重新加载后模板集合与当前选择保持不变。
- [x] 1.2 扩展仓储测试，分别固定缺失模板集合保留现有定义与显式空集合执行既有删除校验的语义。
- [x] 1.3 为前端 API 包装增加测试：缺失模板集合或仅有当前模板标识时不得构造空模板集合请求。
- [x] 1.4 为设置界面增加交互测试：保留已有模板时修改非模板设置、保存并重新打开设置，任务模板仍显示。

## 2. 保存语义修复

- [x] 2.1 调整 `App.SaveSettings`，在完整设置校验前读取当前设置，并在模板集合未提供时补回模板集合与当前模板标识。
- [x] 2.2 保持仓储层的缺失模板保护与显式模板更新校验，确认显式空集合仍遵守活动任务引用限制。
- [x] 2.3 调整前端 `saveSettings` 请求构造，使模板集合与当前模板标识只作为完整快照一起提交，不让单独标识伪造空集合。
- [x] 2.4 补全设置草稿的模板快照初始化路径，确保局部设置编辑不会丢失已加载的模板集合或当前选择。

## 3. 验证

- [x] 3.1 运行相关 Go 测试及 `go test ./...`，确认普通设置、显式模板删除和模板引用保护均通过。
- [x] 3.2 运行前端 `npm test` 与 `npm run build`，确认设置重开回填和 Wails 请求类型兼容。
