## 1. 执行记录模型与持久化

- [x] 1.1 为 `LifecycleExecution` 增加运行标识和进度版本，并兼容缺少新字段的历史快照。
- [x] 1.2 扩展生命周期执行记录的条件更新能力，使旧运行或较低版本不能覆盖当前记录；补充模型、仓储与启动中断迁移测试。
- [x] 1.3 修复失败记录复制当前命令 ID、名称、序号和总数的逻辑，并覆盖第 2 条命令失败与应用重启中断场景。

## 2. 后台生命周期状态机

- [x] 2.1 将同步 `runLifecycleHook` 拆分为同步调度、冻结执行请求和后台工作器，确保运行记录持久化后绑定立即返回。
- [x] 2.2 为任务级调度与续接增加互斥保护，并在进度、成功和失败时按运行标识与版本条件持久化及发布事件。
- [x] 2.3 实现 `beforeStart`、`postStart`、`beforeEnd`、`postEnd` 与 `updateTask` 的后台续接流程，保持既有状态变更、实时状态注册和失败不回滚语义。
- [x] 2.4 调整失败重试入口，使其创建新的运行标识、读取最新链定义并复用相应钩子的后台续接流程。
- [x] 2.5 使用可阻塞命令执行器补充应用层测试，验证开始、结束、更新和重试在命令完成前即返回运行快照，并验证逐步进度、失败定位、成功解锁和链删除错误。

## 3. 前端进度与交互

- [x] 3.1 更新 Wails 映射类型和前端任务类型，支持运行标识与进度版本。
- [x] 3.2 实现生命周期任务快照合并函数，保证同一运行的低版本绑定返回或事件不会覆盖较新进度，新重试运行可以替换旧失败运行。
- [x] 3.3 调整任务保存、开始、结束和重试处理：执行中任务保存响应后立即关闭编辑表单；任务状态跨未执行、执行中、已完成标签时保持进度可见。
- [x] 3.4 调整任务树和详情的执行状态呈现：运行时显示非错误的“钩子 · 命令 序号/总数”，失败时准确标红失败命令并显示重试；执行或失败期间持续禁用全部非重试操作。
- [x] 3.5 补充前端测试，覆盖运行中无重试、失败步骤名称和红色状态、菜单禁用、保存弹窗立即关闭，以及响应与事件乱序不回退。

## 4. 回归验证

- [x] 4.1 运行 `go test -race ./...`，修复生命周期状态机、持久化和 HTTP 详情相关回归。
- [x] 4.2 运行 `cd frontend && npm test && npm run build`，修复前端类型、事件合并和任务树交互回归。
- [x] 4.3 运行 `./scripts/build-linux.sh` 完成项目编译，并确认构建产物生成成功。
