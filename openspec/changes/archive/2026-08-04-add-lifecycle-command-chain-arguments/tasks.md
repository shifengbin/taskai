## 1. 生命周期命令模型与配置迁移

- [x] 1.1 为 `settings.LifecycleCommand` 增加可归一化的链级参数模式，并为允许、禁止和缺失旧值实现校验及规范化。
- [x] 1.2 更新三个系统生命周期命令的固定模式：创建/删除工作目录禁止，Git 仓库克隆允许。
- [x] 1.3 为设置和存储增加测试，覆盖旧配置迁移为允许、未知模式拒绝、固定参数保留及链引用参数不被改写。

## 2. 命令链保存与执行兼容

- [x] 2.1 保持命令链引用参数的规范化、复制和保存与参数模式解耦，确保关闭模式后已有链仍可保存且参数不丢失。
- [x] 2.2 为应用层补充回归测试，验证命令以“固定参数后接链级追加参数”的顺序执行，以及禁止模式不影响既有链的执行参数。
- [x] 2.3 更新 Wails 生成绑定，使生命周期命令对前端暴露归一化后的链级参数模式。

## 3. 生命周期编排界面

- [x] 3.1 扩展前端 `LifecycleCommand` 类型和命令草稿，在新增/编辑自定义命令弹窗中提供“允许在命令链中追加参数”复选框，并将已有参数字段标为“固定参数”。
- [x] 3.2 让新建命令草稿默认禁止链级追加参数；编辑既有命令时正确回显其已保存模式。
- [x] 3.3 在命令链编辑器中仅为允许模式的命令引用渲染“追加参数”输入框；隐藏时保持草稿和提交请求中的已有参数不变，重新允许后恢复显示。
- [x] 3.4 补充前端交互测试，覆盖允许/禁止显隐、固定参数标签、关闭后历史链参数保留及 Git 仓库克隆参数框仍可用。

## 4. 文档与验证

- [x] 4.1 用中文更新 README，说明固定参数、链级追加参数、开关的编辑范围与历史链保留规则。
- [x] 4.2 运行针对性 Go 与 Vitest 测试，再运行 `go test -race ./...`、`npm test` 和 `npm run build`。
- [x] 4.3 运行 `./scripts/build-linux.sh`，检查 Wails 绑定与 Linux 构建结果。
- [x] 4.4 运行 `openspec validate add-lifecycle-command-chain-arguments --strict` 与 `git diff --check`，确认提案工件和实现变更均符合规范。
