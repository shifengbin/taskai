## 1. 隔离开发工作区

- [x] 1.1 在任务目录的 `.worktrees` 中创建本变更的 Git worktree 和功能分支，并确认主工作区现有变更不会被覆盖
- [x] 1.2 在 worktree 中读取适用的项目约束，运行仓库 Git 服务、设置和 Windows 进程相关测试建立基线

## 2. 统一后台进程窗口策略

- [x] 2.1 先增加 Windows 构建测试，断言任务 Git 仓库服务创建的查询与操作 Git 进程设置 `HideWindow` 和 `CREATE_NO_WINDOW`，且仍保留工作目录、环境变量和标准流
- [x] 2.2 将平台相关后台进程配置提取到通用内部组件，并迁移生命周期执行器、任务菜单后台命令和仓库 Git 服务调用方
- [x] 2.3 运行 Linux 当前平台测试与 Windows 交叉构建测试，确认内嵌终端不经过后台进程配置且既有命令失败信息不丢失

## 3. 修复仓库发现与失败隔离

- [x] 3.1 先增加失败测试，覆盖普通目录不启动 Git、`.git` 目录普通仓库、`.git` 文件 worktree、嵌套仓库、无效候选和工作目录外候选
- [x] 3.2 将目录遍历改为收集 `.git` 标记的父目录候选，并仅对去重候选执行 Git 顶层目录与真实路径边界验证
- [x] 3.3 先增加失败测试，覆盖一个仓库基础状态读取失败时仍返回错误卡片和其他有效仓库
- [x] 3.4 调整状态聚合，使无效候选被跳过、有效仓库的基础状态失败转换为带相对路径和 `notice` 的 `ActionNone` 结果
- [x] 3.5 运行 `internal/repositorygit` 测试，确认提交、发布、同步、远程选择、深度限制和路径安全既有行为保持不变

## 4. 对齐默认扫描深度

- [x] 4.1 先增加设置与存储失败测试，覆盖新默认深度 3、旧默认值 2 的一次性迁移、非默认值保留以及迁移后显式改回 2
- [x] 4.2 将默认 Git 最大扫描深度改为 3，并增加设置预置迁移版本，仅把旧版本中的旧默认值 2 更新为 3
- [x] 4.3 增加仓库服务或应用层集成测试，验证默认深度可以发现 `<任务工作目录>/workspaces/<项目>`，显式深度 2 仍不进入第 3 层
- [x] 4.4 更新前端设置默认回退值和提示文本，并验证保存范围仍为 1–10且无需变更 Wails 类型结构

## 5. 验证、集成与交付

- [x] 5.1 运行最小相关 Go 与前端测试，再运行 `go test -race ./...`、`cd frontend && npm test && npm run build`
- [x] 5.2 执行 Windows 目标交叉构建及 Windows 专用测试构建，确认平台文件和后台进程属性可编译
- [x] 5.3 使用 `wails dev` 启动应用且不禁用终端颜色、不自动退出，通过浏览器验证 Git 标签能发现默认 `workspaces/<项目>` 仓库、普通目录不产生额外 Git 调用提示、错误仓库不遮蔽正常仓库
- [ ] 5.4 使用项目脚本编译 Windows 可执行程序并打开程序，在 Windows 上确认点击 Git 标签和执行受控 Git 操作不弹黑框
- [x] 5.5 将当前工作区分支合并到 worktree 功能分支并解决冲突，等待用户确认后再把功能分支合并回当前项目分支
- [x] 5.6 合并后重新编译并验证项目，更新中文设计与实施文档，完成 OpenSpec 同步和归档
- [x] 5.7 提交全部 Git 变更并移除已合并的 worktree

> Windows 可执行程序已完成交叉编译并通过 Wine 启动验证；5.4 的 Windows 真实桌面黑框验证按用户安排在其他 Windows 系统执行，不阻塞本次 Linux 交付归档。
