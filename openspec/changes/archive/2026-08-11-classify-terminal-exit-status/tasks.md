## 1. 退出结果与后端分类

- [x] 1.1 为终端会话和管理器补充失败测试，覆盖自然零退出码、非零退出码、无有效退出码及受控关闭原因优先级。
- [x] 1.2 扩展共享终端会话契约以返回结构化退出结果，并让 Unix 与 Windows 后端从各自的 `ProcessState` 采集可用退出码。
- [x] 1.3 在终端管理器中按退出结果发布 `normal` 或 `unexpected`，保留受控关闭原因并在事件中传递可用退出码。
- [x] 1.4 补充 Unix 后端的正常与非零退出验证，并检查 Windows 测试和构建约束仍与共享契约一致。

## 2. 实时状态与前端终端条目

- [x] 2.1 为应用层增加失败测试，覆盖正常结束移除实时状态、异常结束标为错误，以及关闭已退出异常终端后清除状态。
- [x] 2.2 更新应用层退出事件和 `CloseTerminal` 处理，使正常与受控结束移除实时投影，异常保留到用户关闭。
- [x] 2.3 为前端状态归约增加测试，覆盖带退出原因和退出码的事件、正常结束自动移除、异常条目保留和关闭后的任务状态收敛。
- [x] 2.4 更新前端终端事件类型与状态归约，使前端仅按后端退出原因决定移除或保留，不自行推断退出结果。

## 3. 异常输出快照

- [x] 3.1 为前端终端会话注册表和终端视图补充失败测试，覆盖异常退出后保留输出、正常结束释放，以及已冻结快照拒绝输入、粘贴、快捷键、文件拖放和尺寸更新。
- [x] 3.2 实现异常退出 xterm 会话的只读冻结，保留既有 1000 行滚屏；在用户关闭、任务结束和前端卸载时释放快照。
- [x] 3.3 调整终端视图交互，使异常快照可查看但不触发任何后端终端操作。
- [x] 3.4 在异常快照末尾追加独立的“终端已退出”提示行，并隐藏 xterm 光标。

## 4. 规格与验证

- [x] 4.1 确认实现与本 change 的退出分类和输出保留规格一致，并同步完成状态与必要的中文文档。
- [x] 4.2 运行受影响的 Go 包、应用层测试和前端状态/终端会话测试。
- [x] 4.3 运行 `go test -race ./...`、`cd frontend && npm test && npm run build` 以及 `openspec validate classify-terminal-exit-status --strict --no-interactive`。
- [x] 4.4 使用 `scripts` 中的 Linux 构建脚本编译并启动程序，手动验证 `exit 0` 自动移除，以及失败命令保留可查看的只读输出快照；异常快照末尾显示独立的“终端已退出”提示且不显示光标；启动时不得禁用终端颜色。
