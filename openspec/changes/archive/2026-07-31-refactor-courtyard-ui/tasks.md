## 1. 庭院主题基础

- [x] 1.1 在 `frontend/src/App.tsx` 中将亮色与暗色 MUI 调色令牌替换为岩灰、鼠尾草绿、受控黏土色和低照度墨绿层级。
- [x] 1.2 调整 `createAppTheme` 的字体层级、焦点环与 MUI 组件覆盖，移除松林夜跑的荧光色、斜纹、硬阴影和不规则圆角。
- [x] 1.3 更新 `frontend/src/App.css` 的工作台、顶栏、侧栏、分隔条、详情、空状态和启动屏为庭院布局细节，并保留减少动态效果支持。

## 2. 工作台与终端视觉

- [x] 2.1 调整 `frontend/src/components/TaskTree.tsx` 的任务行、选中态、终端子项、拖拽预览和插入反馈，使任务自定义颜色与实时状态保持可辨认且不改变交互逻辑。
- [x] 2.2 调整任务详情相关的局部 `sx` 样式，使标题、章节、信息卡和生命周期反馈与庭院令牌一致。
- [x] 2.3 调整 `frontend/src/components/TerminalView.tsx` 的终端主题与标题栏，使亮暗模式均保持深森林终端、可读前景、光标和选择区。
- [x] 2.4 走查任务编辑、额外信息、模板、设置、菜单、提示和确认对话框，补齐未继承庭院主题的 MUI 覆盖。

## 3. 回归保护与验证

- [x] 3.1 更新或补充应用、任务树和终端组件测试，覆盖现有选择、状态切换、拖拽、终端标题与关闭等行为未受视觉变更影响。
- [x] 3.2 在亮色和暗色模式下检查关键文字、焦点、错误/警告、任务状态与终端前景的可读性，并验证 `prefers-reduced-motion` 下保留静态反馈。
- [x] 3.3 运行 `cd frontend && npm test && npm run build`，修复本变更引入的失败后记录结果。
