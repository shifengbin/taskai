## 1. 终端输出帧化

- [x] 1.1 在终端管理器的每个受管会话中维护未完成的 UTF-8 输出尾字节，并仅发布已构成完整字符的输出。
- [x] 1.2 在读取返回错误或终端关闭前刷出尾缓冲，保证输出事件先于错误和退出事件，且无法补全的字节不会阻塞会话结束。
- [x] 1.3 保持现有终端 ID 路由、输入写入、活动状态通知和 Unix/Windows 平台后端契约不变。

## 2. 回归测试

- [x] 2.1 为受管终端添加强制分片测试，验证被拆分的中文字符会按原字符发布且不产生替换字符。
- [x] 2.2 添加 emoji、多次分片、ANSI 控制序列和终端结束时残留尾字节的测试，验证输出顺序和结束事件顺序。
- [x] 2.3 运行 `go test ./internal/terminal`，确认终端管理器既有行为和新增分片行为均通过。

## 3. 完整验证

- [x] 3.1 运行 `go test -race ./...`，检查共享终端读取循环在并发下的回归。
- [x] 3.2 运行 `cd frontend && npm test && npm run build`，确认既有终端事件契约继续通过前端类型检查、测试和构建。
