## 1. 领域模型与迁移基础

- [x] 1.1 为模板字段默认值、可复用信息和任务级动态参数扩展 `internal/task` 领域模型，并编写先失败的模型测试。
- [x] 1.2 实现 `name` 固定字段约束、字段/参数键冲突校验、任务固定字段只读来源校验及动态参数必填校验。
- [x] 1.3 实现内置 Git 模板的受保护字段规则和非内置模板自动 `name` 字段规则，并覆盖模型测试。
- [x] 1.4 在 `internal/storage` 数据结构中分离模板定义集合和信息集合，保留旧 JSON 的反序列化兼容字段。
- [x] 1.5 实现幂等加载迁移：当前单层模板转模板与信息、不同结构拆分模板、补种 Git 模板且不重建历史任务快照。
- [x] 1.6 覆盖空数据、旧单键模板、当前单层模板、同分类不同结构及迁移幂等性的仓储测试。

## 2. 后端服务与任务快照

- [x] 2.1 为模板和信息新增列表、保存、删除应用契约、仓储方法和 Wails `App` 绑定。
- [x] 2.2 实现模板存在关联信息时禁止删除、信息删除不影响任务、Git 模板不可删除及字段保护的服务端校验。
- [x] 2.3 更新创建和编辑任务流程：从信息和模板组装固定字段快照及动态参数快照，并仅允许任务级动态参数变更。
- [x] 2.4 更新任务详情 HTTP 映射，继续按 `catalogue` 分组并平铺固定字段、模板参数和任务级动态参数值。
- [x] 2.5 为应用绑定、生命周期服务和 HTTP 行为补充单元测试，覆盖历史快照与已删除来源的编辑场景。

## 3. 前端管理与任务表单

- [x] 3.1 更新 TypeScript 类型和 `frontend/src/api.ts`，区分模板、信息和任务快照；重新生成 Wails 绑定并检查生成结果。
- [x] 3.2 重构额外信息管理界面，提供模板列表、内置 Git 模板保护提示、模板字段/参数编辑及默认值输入。
- [x] 3.3 实现基于模板创建、编辑、删除信息的表单；以 `name` 值展示信息并用模板默认值初始化固定字段。
- [x] 3.4 更新任务创建和编辑表单：按模板分类选择信息、只读渲染固定字段、编辑模板参数并支持任务级新增动态参数。
- [x] 3.5 保留跨分类已选信息和已删除来源快照的编辑能力，并为名称、键冲突和必填动态参数提供可见错误反馈。
- [x] 3.6 更新 Vitest 测试，覆盖 Git 默认值、信息选择、固定字段只读、动态参数变更、任务级新增参数和迁移后数据渲染。

## 4. 文档与验证

- [x] 4.1 更新 README 中的额外信息说明，描述模板、信息、任务快照与 Git 内置模板的实际使用流程。
- [x] 4.2 执行 Go 单元测试与竞态测试、前端测试及前端生产构建，修复发现的问题。
- [x] 4.3 执行 `openspec validate refine-extra-info-template-flow --strict --no-interactive`、`git diff --check` 和 `./scripts/build-linux.sh`，确认构建产物存在。

## 5. 信息级动态参数与分类稳定展示

- [x] 5.1 为 `ExtraInfo` 增加可持久化的动态参数默认值，并以领域模型测试覆盖参数合成、冲突校验和旧数据兼容。
- [x] 5.2 更新任务快照创建服务，使模板参数、信息级参数和任务级新增参数按来源校验与合成，并覆盖应用绑定测试。
- [x] 5.3 更新 Wails 绑定模型、前端类型和信息创建/编辑表单，使信息可新增、编辑和删除自身的动态参数。
- [x] 5.4 调整管理界面：分类模板整体可折叠、模板行响应式布局、搜索保留所有分类分组并自动展开命中分组；补充前端交互测试。
- [x] 5.5 运行 Go、前端、OpenSpec 严格校验与项目 Linux 构建脚本，并更新 README 的信息参数使用说明。

## 6. 分类模板完整展示修复

- [x] 6.1 修复低高度窗口中模板面板被后续信息区域覆盖的问题，增加前端回归测试并完成全量验证。

## 7. 动态参数复选框类型

- [x] 7.1 为模板、信息和任务快照参数增加输入类型、布尔值归一化及必填规则，并补充 Go 回归测试。
- [x] 7.2 在模板、信息和任务动态参数编辑器中提供复选框类型选择与布尔控件，补充 Vitest 交互测试。
- [x] 7.3 重新生成 Wails 绑定，更新 README、OpenSpec 文档并完成全量构建验证。

## 8. 任务 HTTP 复选框响应类型

- [x] 8.1 将任务详情 HTTP 响应中的复选框动态参数输出为 JSON 布尔值，并保持固定字段和文本参数的字符串语义。
- [x] 8.2 补充 HTTP 回归测试、README 接口说明并完成完整验证。

## 9. 任务 HTTP 活动终端详情

- [x] 9.1 为终端管理器增加活动会话命令快照，并补充普通 Shell 与自定义命令的测试。
- [x] 9.2 在任务详情 HTTP 响应中输出终端 ID、命令和实时状态；保持任务列表响应不变并补充回归测试。
- [x] 9.3 更新 README、OpenSpec 文档并完成完整验证。
