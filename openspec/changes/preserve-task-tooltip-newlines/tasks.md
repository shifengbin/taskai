## 1. 回归测试

- [x] 1.1 为多行并带缩进的任务描述补充悬浮提示测试，并断言提示保留完整文本且使用保留空白样式。
- [x] 1.2 运行 `TaskTree` 目标测试，确认新增断言在现有实现下失败。

## 2. 悬浮提示呈现

- [x] 2.1 在任务描述悬浮提示容器上应用保留换行、连续空白和自动折行的样式。
- [x] 2.2 重跑 `TaskTree` 目标测试，确认多行描述、长行和空描述回退行为通过。

## 3. 完整验证

- [x] 3.1 运行前端完整测试与生产构建，以及 Go 测试。
- [x] 3.2 严格校验 OpenSpec 变更，并运行 Linux 打包构建后打开可执行程序供验收。
