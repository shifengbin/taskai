## 1. 数据模型与持久化

- [x] 1.1 在任务领域模型中定义五个生命周期钩子、任务链选择、链执行记录及其验证和 JSON 序列化测试。
- [x] 1.2 在设置模型中定义生命周期命令、命令链、默认链映射和两个不可变系统目录命令，并实现规范化与验证测试。
- [x] 1.3 扩展存储仓库的加载迁移逻辑，补齐系统命令、初始目录链、默认值、旧任务选择和执行记录，并覆盖未执行、执行中、已完成快照迁移测试。
- [x] 1.4 实现命令与链的仓库读写、复制和删除约束，覆盖命令被链引用、链被未执行/执行中任务引用、已完成任务链删除和默认值清理测试。

## 2. 命令链执行基础设施

- [x] 2.1 将任务工作区创建改为可安全复用的操作，并保留删除不存在目录成功、路径穿越和符号链接防护测试。
- [x] 2.2 新建生命周期命令链执行器，按选中 Shell、工作目录和任务环境串行运行自定义命令，捕获退出错误及标准错误诊断。
- [x] 2.3 实现原样标准输入输出管道：构建首命令任务详情 JSON，按服务可用性注入顶层 `baseURL`，并将每步输出原样传给下一步。
- [x] 2.4 在执行器中实现创建/删除目录内置命令、输出透传、命令顺序工作目录规则和从链首重试的幂等行为测试。
- [x] 2.5 实现持久化执行记录、每命令进度事件、失败记录和应用重启中断标记，并提供任务锁定查询。

## 3. 生命周期与应用绑定

- [x] 3.1 重构开始任务流程：运行 `beforeStart` 后才提交执行中状态和目录快照，再运行 `postStart`，并覆盖两类失败状态测试。
- [x] 3.2 重构结束任务流程：运行 `beforeEnd` 后关闭终端并提交完成状态，再运行 `postEnd`，移除隐式目录删除并覆盖两类失败状态测试。
- [x] 3.3 调整任务更新流程，仅在执行中任务保存成功后运行 `updateTask`，并覆盖未执行/已完成任务不触发和失败不回滚测试。
- [x] 3.4 为命令、链、默认值、任务链选择、链重试和执行记录增加 Go/Wails 绑定，并在所有任务操作入口实施后端锁定保护。
- [x] 3.5 扩展任务详情 HTTP 资源和应用上下文构造，验证 `beforeStart` 的预期目录、运行时 `baseURL` 和 `postEnd` 根目录行为。

## 4. 前端命令链管理与任务交互

- [x] 4.1 扩展 Wails API 封装、前端类型和事件处理，接收命令、链、任务链选择及执行状态数据。
- [x] 4.2 在设置界面新增命令管理，支持自定义命令新建、编辑、删除、系统命令只读展示及引用错误提示。
- [x] 4.3 在设置界面新增链管理，支持按名称创建、编辑、复制、拖动排序、删除和逐钩子设置默认链。
- [x] 4.4 在新建和编辑任务表单加入五个链下拉选择器，按默认值预选并保存用户修改。
- [x] 4.5 在任务树显示钩子、当前命令和序号；运行/失败时禁用右键、菜单、编辑、排序和状态变更，并在失败时提供重试及“链已删除”反馈。

## 5. 回归验证与文档

- [x] 5.1 补充并通过后端生命周期、存储迁移、执行器、目录安全、HTTP 上下文和应用绑定的单元测试。
- [x] 5.2 补充并通过前端命令链管理、任务链选择、进度展示、锁定和重试交互测试。
- [x] 5.3 更新项目 README，说明命令链、五个钩子、标准输入输出、目录内置命令、失败重试和默认链配置。
- [x] 5.4 运行 `scripts` 中的目标平台构建脚本，并记录构建结果。

## 6. 命令链适用范围增量

- [x] 6.1 为命令和链增加钩子范围、系统目录命令固定范围、旧数据迁移及范围覆盖校验。
- [x] 6.2 限制默认链和任务新建选择为适用链，禁止编辑任务修改链选择，并保护被活动任务引用的链范围。
- [x] 6.3 为范围管理、链命令筛选、默认/新建筛选和编辑只读补充前后端回归测试并完成构建验证。

## 7. 未执行任务编辑命令链增量

- [x] 7.1 恢复仅允许未执行任务修改链选择的后端与 Wails 绑定，并拒绝执行中和已完成任务的直接调用。
- [x] 7.2 按编辑任务状态启用命令链选择器，未执行可编辑、执行中和已完成只读。
- [x] 7.3 补充三种状态的回归测试，更新中文说明并完成严格校验和构建验证。
