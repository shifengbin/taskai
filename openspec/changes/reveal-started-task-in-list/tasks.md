## 1. 任务列表定位

- [x] 1.1 为 `TaskTree` 的可滚动任务列表建立 DOM 引用，并在开始反馈令牌更新、执行中目标条目渲染后执行绘制前的可见性判断。
- [x] 1.2 当目标条目被列表上边缘或下边缘裁切时，仅以最近可见策略调整列表 `scrollTop`；目标已完整可见或不在执行中列表时保持滚动位置不变。
- [x] 1.3 保持现有开始反馈令牌、执行中标签切换、任务选择和 700ms 清理行为，确保定位不会滚动应用窗口或影响其他状态迁移。

## 2. 反馈与可访问性

- [x] 2.1 确保列表定位在反馈首个可见画面前完成，默认偏好下继续仅让目标任务闪烁两次。
- [x] 2.2 在 `prefers-reduced-motion: reduce` 下执行即时列表定位并保留目标任务的静态高亮，不引入平滑滚动。

## 3. 自动化验证

- [x] 3.1 补充任务树组件测试：目标位于列表上方或下方时滚动至完整可见，且仅影响列表自身滚动位置。
- [x] 3.2 补充任务树组件测试：目标已完整可见、目标未渲染和减少动态效果分支不产生非预期滚动，并保留正确反馈标识。
- [x] 3.3 补充应用回归测试：即时开始响应和生命周期事件确认开始时均向任务树传递同一定位反馈令牌。
- [x] 3.4 运行 `cd frontend && npm test && npm run build`。

## 4. 项目编译

- [x] 4.1 运行 `./scripts/build-linux.sh`，确认 Linux 应用构建成功并生成 `build/bin/taskai`。
