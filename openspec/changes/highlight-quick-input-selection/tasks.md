## 1. 快捷输入选中态

- [x] 1.1 在 `TerminalView` 中以现有快捷输入选中索引驱动结果项的主题色弱背景和左侧强调线，并保持无障碍选中语义、悬停同步和搜索框焦点行为。

## 2. 验证

- [x] 2.1 扩展终端快捷输入键盘导航测试，验证方向键切换后仅当前结果拥有可见选中状态，且搜索输入框仍保持焦点。
- [x] 2.2 运行受影响的前端测试，确认搜索、Enter 插入、Escape 关闭和终端焦点恢复未回归。
