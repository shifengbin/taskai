## 1. 开发隔离与基线

- [x] 1.1 在任务目录 `.worktrees` 下创建 `fix-terminal-cursor-visibility` worktree 和功能分支，并确认主工作区、worktree 路径及分支状态正确
- [x] 1.2 在功能分支运行 `cd frontend && npm test -- terminal-session.test.ts`，记录当前基线结果并确认没有引入本变更之外的失败

## 2. 回归测试

- [x] 2.1 扩展终端会话测试桩以记录同步输出状态和生命周期，并增加断言确认应用层不再写入合成 `ESC[?25l` / `ESC[?25h` 序列
- [x] 2.2 增加输出突发在静默冲刷、64 毫秒截止冲刷和 1MB 上限冲刷之间保持同步状态的测试，并验证输出顺序和 `onWriteParsed` 行为不变
- [x] 2.3 增加真实 xterm parser 测试，覆盖重绘后普通文本、字符输入、左右方向键及程序主动隐藏光标，验证光标最终可见性和程序控制序列语义
- [x] 2.4 增加浏览器集成测试，使用 `.xterm-cursor` 采样位置、可见性和闪烁节奏，并覆盖后台会话切换和会话销毁

## 3. 终端会话实现

- [x] 3.1 从 `frontend/src/terminal-session.ts` 删除合成光标隐藏/恢复字段、常量、定时器和写入分支，保留现有输出队列的静默、截止、上限及顺序行为
- [x] 3.2 为每个终端会话实现渲染同步状态，在首次冲刷时进入同步输出，跨越截止冲刷持续保持，并在最后输出静默后先冲刷剩余数据再解除同步
- [x] 3.3 处理同步状态的所有权、xterm 安全超时和终端程序自身同步模式，确保注册器只关闭自己建立的状态并能在需要时重新建立
- [x] 3.4 在退出、分离、重新附着、销毁和异常路径清理同步定时器与待处理队列，确保退出快照最终渲染且已销毁会话不再写入
- [x] 3.5 确认 `output-change` 的画面快照比较、活动状态计时、后台会话解析和终端输入 API 未改变，并由相关测试验证

## 4. 局部验证与审查

- [x] 4.1 运行 `cd frontend && npm test -- terminal-session.test.ts` 及新增的 xterm/浏览器测试，确认光标消失、闪烁异常和方向键回归用例通过
- [x] 4.2 运行 `cd frontend && npm run build`，确认 TypeScript 检查和 Vite 构建成功
- [x] 4.3 对功能分支执行代码审查，重点检查同步状态生命周期、定时器竞态、程序光标控制序列兼容性及跨平台路径，并在修订后重跑局部测试

## 5. 集成验证与交付构建

- [x] 5.1 将主工作区分支的最新提交合并到 worktree 功能分支，解决冲突后运行受影响测试并确认合并结果
- [x] 5.2 使用 `wails dev` 启动应用，从输出获取调试地址并用浏览器工具验证高频重绘、普通输出、左右键输入、闪烁节奏、后台切换和退出收尾；测试完成后关闭调试进程
- [x] 5.3 在可用的 Windows 和 Linux 环境分别运行高频重绘脚本，确认两平台均无永久隐藏光标或异常闪烁；记录无法提供的平台验证结果
- [x] 5.4 使用项目编译脚本构建可执行程序并打开应用，确认构建产物可启动且终端交互正常

## 6. 合并、文档与归档

- [ ] 6.1 完成集成验证后等待用户确认；确认无误后将 worktree 功能分支合并回当前工作区对应分支，并再次运行前端构建验证合并结果
- [ ] 6.2 将最终设计和验证结果同步到中文 `docs/plans/` 实施记录及 `openspec/specs/terminal-output-frame-batching/spec.md`，运行 `openspec validate fix-terminal-cursor-visibility --strict`
- [ ] 6.3 提交全部 Git 变更，记录测试与构建结果，并确认提交只包含本变更相关文件
- [ ] 6.4 移除已合并的 `.worktrees` 目录和 worktree，运行 `git status --short` 确认工作区状态符合预期
