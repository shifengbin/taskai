## 1. 隔离开发环境与失败测试

- [x] 1.1 在任务目录创建 `.worktrees`，从当前工作区对应分支创建 `unify-terminal-mouse-selection` worktree 分支，并确认现有 `add-build-release-workflow` 变更不被修改。
- [x] 1.2 扩展 `TerminalSessionRegistry` 的 xterm 模拟和失败测试，覆盖普通模式与 `mouseTrackingMode` 非 `none` 时的单击延迟重放、阈值内抖动、超过阈值拖拽、双击单词、三击整行、滚轮/中键透传，以及失活、切换、关闭和卸载取消待处理事件。
- [x] 1.3 扩展 `TerminalView` 失败测试，覆盖拖拽、双击和三击选择完成后自动复制并显示同一原文的备注入口，单击空选区不显示入口，以及右键仅向当前会话模拟粘贴。
- [x] 1.4 添加 Go 和前端失败测试，锁定菜单设置、保存 payload、终端记录与生成绑定不再包含 `disableTaskAIMouseClipboard`，并验证带旧字段的设置 JSON 仍可加载、再次保存后字段消失。

## 2. 统一终端鼠标手势

- [x] 2.1 在终端会话层实现可释放的左键手势分类器，集中管理多击窗口、拖拽阈值、捕获阶段监听器、计时器和会话生命周期清理。
- [x] 2.2 实现带内部标记的 DOM 事件重放：单击按原始坐标交给 xterm；拖拽、双击和三击使用跨平台的 xterm 强制选择语义，且选择手势不得作为鼠标输入写入 PTY。
- [x] 2.3 从会话层发布选择完成文本和结束位置，使 `TerminalView` 在下一动画帧显示边界内的备注按钮，并保留现有空选区、视图切换、终端失活及点击其他区域的隐藏行为。
- [x] 2.4 保留所有活动终端的选区自动复制和右键模拟粘贴，确认滚轮、中键、其他按钮、键盘输入、终端输出、快捷输入和文件拖放仍沿用原行为。

## 3. 删除旧鼠标策略设置

- [x] 3.1 从 Go 设置模型、默认值、校验和任务菜单保存逻辑中删除 `disableTaskAIMouseClipboard`，并从终端创建与事件数据中删除对应策略快照。
- [x] 3.2 从前端类型、任务菜单草稿、保存映射和设置界面删除“禁用 TaskAI 鼠标复制与右键粘贴”控件及所有条件判断。
- [x] 3.3 运行 `wails generate module` 重新生成前端绑定，检查 `frontend/wailsjs/` 差异仅包含预期字段移除，并执行旧设置兼容测试。
- [x] 3.4 更新受影响的测试夹具和断言，删除只验证禁用分支的用例，保留并加强统一选择、复制、粘贴和备注行为的覆盖。

## 4. 自动化验证

- [x] 4.1 运行最小相关前端测试：`cd frontend && npm test -- terminal-session.test.ts components/TerminalView.test.tsx App.test.tsx`，修复失败后重复执行直至通过。
- [x] 4.2 运行受影响 Go 测试：`go test ./internal/settings ./internal/terminal ./...`，并确认旧 JSON 兼容、终端模型和应用编排测试通过。
- [x] 4.3 运行完整验证：`go test -race ./...`，以及 `cd frontend && npm test && npm run build`；任何失败均定位原因、修复并重新执行完整命令。
- [x] 4.4 对共享鼠标和终端契约检查 Unix 与 Windows 实现及测试，确保没有引入平台专用路径、Shell、信号、修饰键或进程假设。

## 5. Wails 集成测试

- [x] 5.1 使用 `wails dev` 持续运行应用，不设置 `NO_COLOR` 等禁色变量、不让进程自动退出；从输出记录调试地址，并使用 Chrome DevTools 打开该地址。
- [x] 5.2 在普通 Shell 终端中验证：单击聚焦但不产生 PTY 鼠标输入；拖拽选择范围、双击选择单词、三击选择整行均自动复制并在正确位置显示备注按钮；保存备注后标题栏数量与原文正确；右键仅把剪贴板内容粘贴到当前终端。
- [x] 5.3 在 Claude 或 Codex 等真实开启鼠标追踪的终端中验证：单击仍可操作终端界面；滚轮与中键行为仍交给终端程序；拖拽、双击和三击只创建本地选区、不改变终端程序焦点或选项，并可完成备注保存、复制和发送。
- [x] 5.4 使用 Chrome DevTools 检查上述手势期间页面无控制台错误，备注按钮不越过内容区、不与其他界面重叠，并在终端切换、关闭和失活后无迟到点击或残留按钮；功能测试完成后关闭 `wails dev` 调试进程。

## 5A. 用户确认后的选区保持回归

- [x] 5A.1 添加鼠标追踪期间选择后仅移动鼠标的失败测试，锁定移动事件仍发送给 PTY，但同一本地选区与备注入口保持可用。
- [x] 5A.2 使用 xterm 公开选区坐标保存并恢复被鼠标移动输入清除的选区，不改变单击、滚轮、中键和右键分流。
- [x] 5A.3 运行相关前端测试和完整验证，并在真实 Claude `mouseTrackingMode: any` 会话中确认拖拽、双击、三击后移动鼠标仍保留选区和备注入口。
- [x] 5A.4 重新构建并启动正式程序，保持运行并等待用户再次确认后再继续合并。

## 6. 编译、确认与交付

- [x] 6.1 使用 `scripts/` 下的项目编译脚本构建可执行程序并打开程序，保持程序运行，等待用户确认统一鼠标选择、备注和粘贴行为无问题。
- [ ] 6.2 用户确认后，将当前工作区对应分支先合并到 worktree 分支并解决冲突，再把已验证的 worktree 分支合并回当前工作区对应分支。
- [ ] 6.3 合并后再次运行项目编译脚本和完整测试，确认合并没有引入编译或行为回归。
- [ ] 6.4 将最终行为同步到正式 OpenSpec 规格与 `docs/plans/` 记录，完成 OpenSpec 归档，提交全部相关 Git 变更并移除已合并 worktree。
