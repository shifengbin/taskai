# 27 Stamp — 孔版印章

## 主题定位

印刷海报。该方向围绕“Riso Poster Pop”组织，适合需要快速浏览任务状态并频繁切换工作上下文的桌面应用。

## 视觉关键词

孔版印章波普：朱砂红、孔雀绿、芥末黄三色撞色，强半色网点 + 邮票般的粗框错位投影——一张快乐的海报。

## 色彩令牌

主色与强调色：`#F2EBD9`、`#161616`、`#E63946`、`#2A9D8F`、`#E9C46A`。页面同时提供浅色与深色样式，具体令牌如下。

| 模式 | 变量 | 值 |
| --- | --- | --- |
| 亮色 | `--st-bg` | `#F2EBD9` |
| 亮色 | `--st-surface` | `#FBF6E8` |
| 亮色 | `--st-surface2` | `#E9DFC4` |
| 亮色 | `--st-ink` | `#161616` |
| 亮色 | `--st-mut` | `#6B6453` |
| 亮色 | `--st-outline` | `#161616` |
| 亮色 | `--st-accent` | `#E63946` |
| 亮色 | `--st-accent2` | `#2A9D8F` |
| 亮色 | `--st-accent3` | `#E9C46A` |
| 亮色 | `--st-accent4` | `#1D3557` |
| 亮色 | `--st-dots` | `rgba(22,22,22,.09)` |
| 亮色 | `--st-work` | `#2A9D8F` |
| 亮色 | `--st-idle` | `#A89F88` |
| 亮色 | `--st-unread` | `#1D3557` |
| 亮色 | `--st-error` | `#E63946` |
| 暗色 | `--st-bg` | `#1A1813` |
| 暗色 | `--st-surface` | `#24211A` |
| 暗色 | `--st-surface2` | `#2E2A21` |
| 暗色 | `--st-ink` | `#F0E9D6` |
| 暗色 | `--st-mut` | `#9C937D` |
| 暗色 | `--st-outline` | `#F0E9D6` |
| 暗色 | `--st-accent` | `#FF5A66` |
| 暗色 | `--st-accent2` | `#2BB89E` |
| 暗色 | `--st-accent3` | `#F0CB6E` |
| 暗色 | `--st-accent4` | `#6FA0D8` |
| 暗色 | `--st-dots` | `rgba(240,233,214,.08)` |
| 暗色 | `--st-work` | `#2BB89E` |
| 暗色 | `--st-idle` | `#5C5446` |
| 暗色 | `--st-unread` | `#6FA0D8` |
| 暗色 | `--st-error` | `#FF5A66` |

## 字体与字重

展示与正文字体分别为：`Rubik`、`Be Vietnam`。主题 CSS 实际声明的字体栈包括 `'Be Vietnam Pro',sans-serif`、`'Rubik',sans-serif`、`'Rubik'`、`'JetBrains Mono',monospace`；本主题覆盖的字重为 `700`、`500`。标题、品牌名、任务名称和等宽终端内容据此形成从扫描标题到密集日志的层级。

## 布局与间距

预览舞台最小宽度为 `880px`，应用窗口高度为 `632px`。顶栏为 `52px`，侧栏为 `300px`；顶栏横向内边距 `16px`，任务行内边距 `10px 12px 10px 10px`，终端输出区内边距 `18px 20px`。窄视口保留横向滚动，不压缩任务与终端的操作密度。

## 主要组件规则

应用框架由顶栏、品牌标记、三段任务标签、任务树、终端标签和终端输出区组成。印刷海报在组件形状上使用圆角 `6px`、`7px`、`8px`、`8px 8px 0 0`，阴影或描边处理为 `9px 9px 0 var(--st-outline)`、`2px 2px 0 var(--st-outline)`、`3px 3px 0 var(--st-outline)`、`3px 3px 0 var(--st-outline), inset 5px 0 0 var(--c)`、`4px 4px 0 var(--st-outline), inset 5px 0 0 var(--c)`。按钮、任务行和终端标签沿用主题变量的前景、表面和强调色，以保持同一视觉语言。

## 状态表现

亮色：工作中 `#2A9D8F`、空闲 `#A89F88`、未读 `#1D3557`、异常 `#E63946`；暗色：工作中 `#2BB89E`、空闲 `#5C5446`、未读 `#6FA0D8`、异常 `#FF5A66`。工作中与未读状态点带有脉冲环；在 `prefers-reduced-motion` 下动画关闭但状态点仍保持可见。

## 交互反馈

任务行和终端项保留悬停反馈，主题按钮点击后只在当前静态预览中切换 `chosen` 状态；不会调用 TaskAI API 或修改持久化数据。截图查询参数可隐藏页面外壳以聚焦当前主题。

## 可读性与无障碍

正文与背景使用主题定义的前景、边框和强调色令牌保持层级区分。交互元素保留可辨识的轮廓、文本标签与状态色，避免仅依赖单一颜色传达状态。

## React + MUI 实现映射

可用 `CssVarsProvider` 承载令牌；应用框架映射为 `AppBar`、`Drawer` 和 `Box`，列表映射为 `List`/`ListItemButton`，任务行为映射为 `Checkbox`、`Chip`、`IconButton` 与 `Tooltip`。主题的色彩令牌应进入 MUI `palette` 和组件变体，布局尺寸保持为显式设计 token。

## 预览实现

本页引用 `../../_shared/preview.css` 与 `../../_shared/preview.js`，仅内联 27 的主题 CSS 与配置，因此可直接以 `file://` 打开，无需生产接口或应用运行时。

## 家族特性

Pop family 共享高饱和主色、粗体展示字和直接的状态反馈；印刷海报以本页令牌、标题字形和背景处理形成独立辨识度。
