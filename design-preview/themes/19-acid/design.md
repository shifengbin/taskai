# 19 Acid — 酸性实验

## 主题定位

酸性实验室。该方向围绕“Acid Lab”组织，适合需要快速浏览任务状态并频繁切换工作上下文的桌面应用。

## 视觉关键词

与 17 号同源的粗框 + 硬投影语言，主调换成电光酸柠檬绿与钴蓝——实验室仪表般的冷峻撞色，激活态用高饱和酸绿填充，黑底白字按钮依旧一目了然。

## 色彩令牌

主色与强调色：`#2347FF`、`#C6F000`、`#15140E`、`#F4F4EE`、`#1FAA4F`。页面同时提供浅色与深色样式，具体令牌如下。

| 模式 | 变量 | 值 |
| --- | --- | --- |
| 亮色 | `--ac-bg` | `#F4F4EE` |
| 亮色 | `--ac-surface` | `#FFFFFF` |
| 亮色 | `--ac-surface2` | `#E9E9DF` |
| 亮色 | `--ac-ink` | `#15140E` |
| 亮色 | `--ac-mut` | `#686A55` |
| 亮色 | `--ac-line` | `rgba(21,20,14,.14)` |
| 亮色 | `--ac-frame` | `#15140E` |
| 亮色 | `--ac-accent` | `#5C9E00` |
| 亮色 | `--ac-accent2` | `#2347FF` |
| 亮色 | `--ac-accent3` | `#15140E` |
| 亮色 | `--ac-work` | `#1FAA4F` |
| 亮色 | `--ac-idle` | `#B7B7A0` |
| 亮色 | `--ac-unread` | `#2347FF` |
| 亮色 | `--ac-error` | `#E8431F` |
| 暗色 | `--ac-bg` | `#0E0E0A` |
| 暗色 | `--ac-surface` | `#1A1A14` |
| 暗色 | `--ac-surface2` | `#23231A` |
| 暗色 | `--ac-ink` | `#EDEDE0` |
| 暗色 | `--ac-mut` | `#8C8C76` |
| 暗色 | `--ac-line` | `rgba(237,237,224,.16)` |
| 暗色 | `--ac-frame` | `#EDEDE0` |
| 暗色 | `--ac-accent` | `#9BE600` |
| 暗色 | `--ac-accent2` | `#5C7BFF` |
| 暗色 | `--ac-accent3` | `#EDEDE0` |
| 暗色 | `--ac-work` | `#39D96B` |
| 暗色 | `--ac-idle` | `#5A5A48` |
| 暗色 | `--ac-unread` | `#5C7BFF` |
| 暗色 | `--ac-error` | `#FF6B45` |

## 字体与文字

主要字体：`Archivo 900`、`IBM Plex Sans`。标题负责建立层级，标签与元数据保持紧凑，以便在任务清单、侧栏和状态区中稳定扫描。

## 布局与组件

预览使用最小高度 `880px` 的舞台，应用窗口宽度为 `632px`。顶栏高度为 `52px`，侧栏宽度为 `300px`；任务列表、筛选项、状态徽标与操作按钮复用同一密度规则。

## 状态与交互

每个页面同时呈现浅色与深色应用窗口。主题切换会更新窗口标题和状态区内容；清单中的选择、复选和标签状态用于展示任务的进行、完成与提醒反馈。

## 可读性与无障碍

正文与背景使用主题定义的前景、边框和强调色令牌保持层级区分。交互元素保留可辨识的轮廓、文本标签与状态色，避免仅依赖单一颜色传达状态。

## React + MUI 实现映射

可用 `CssVarsProvider` 承载令牌；应用框架映射为 `AppBar`、`Drawer` 和 `Box`，列表映射为 `List`/`ListItemButton`，任务行为映射为 `Checkbox`、`Chip`、`IconButton` 与 `Tooltip`。主题的色彩令牌应进入 MUI `palette` 和组件变体，布局尺寸保持为显式设计 token。

## 预览实现

本页引用 `../../_shared/preview.css` 与 `../../_shared/preview.js`，仅内联 19 的主题 CSS 与配置，因此可直接以 `file://` 打开，无需生产接口或应用运行时。

## 家族特性

Hard-shadow family 以硬边框、明确的层叠阴影和高对比状态块建立操作优先级；酸性实验室通过自己的主色、字形和装饰节奏维持可区分性。
