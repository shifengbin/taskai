# 08 HOLO — 全息科技

## 主题定位

全息科技。该方向围绕“Holographic / Prismatic Tech”组织，适合需要快速浏览任务状态并频繁切换工作上下文的桌面应用。

## 视觉关键词

彩虹镀铬、棱镜渐变描边、极光光晕、金属字效——一种会"流动"的科技感，明亮又未来。

## 色彩令牌

主色与强调色：`linear-gradient(135deg,#FF6EC4,#B06BFF)`、`#4ADEDE`、`#7C8CFF`、`#0D0918`、`#F8F9FD`。页面同时提供浅色与深色样式，具体令牌如下。

| 模式 | 变量 | 值 |
| --- | --- | --- |
| 亮色 | `--h-bg` | `rgba(248,249,253,.86)` |
| 亮色 | `--h-surface` | `rgba(255,255,255,.7)` |
| 亮色 | `--h-surface2` | `rgba(246,243,255,.6)` |
| 亮色 | `--h-ink` | `#241A4A` |
| 亮色 | `--h-mut` | `#6F6797` |
| 亮色 | `--h-line` | `rgba(124,92,220,.20)` |
| 亮色 | `--h-work` | `#0E9F6E` |
| 亮色 | `--h-idle` | `#A39CC4` |
| 亮色 | `--h-unread` | `#8B5CF6` |
| 亮色 | `--h-error` | `#E0457E` |
| 暗色 | `--h-bg` | `rgba(13,9,24,.72)` |
| 暗色 | `--h-surface` | `rgba(28,22,48,.6)` |
| 暗色 | `--h-surface2` | `rgba(40,32,66,.5)` |
| 暗色 | `--h-ink` | `#F1ECFF` |
| 暗色 | `--h-mut` | `#9C94C9` |
| 暗色 | `--h-line` | `rgba(150,120,255,.22)` |
| 暗色 | `--h-work` | `#34E0A4` |
| 暗色 | `--h-idle` | `#5A5280` |
| 暗色 | `--h-unread` | `#B89AFF` |
| 暗色 | `--h-error` | `#FF5C8A` |

## 字体与文字

主要字体：`Unbounded`、`Outfit`。标题负责建立层级，标签与元数据保持紧凑，以便在任务清单、侧栏和状态区中稳定扫描。

## 布局与组件

预览使用最小高度 `880px` 的舞台，应用窗口宽度为 `632px`。顶栏高度为 `52px`，侧栏宽度为 `300px`；任务列表、筛选项、状态徽标与操作按钮复用同一密度规则。

## 状态与交互

每个页面同时呈现浅色与深色应用窗口。主题切换会更新窗口标题和状态区内容；清单中的选择、复选和标签状态用于展示任务的进行、完成与提醒反馈。

## 可读性与无障碍

正文与背景使用主题定义的前景、边框和强调色令牌保持层级区分。交互元素保留可辨识的轮廓、文本标签与状态色，避免仅依赖单一颜色传达状态。

## React + MUI 实现映射

可用 `CssVarsProvider` 承载令牌；应用框架映射为 `AppBar`、`Drawer` 和 `Box`，列表映射为 `List`/`ListItemButton`，任务行为映射为 `Checkbox`、`Chip`、`IconButton` 与 `Tooltip`。主题的色彩令牌应进入 MUI `palette` 和组件变体，布局尺寸保持为显式设计 token。

## 预览实现

本页引用 `../../_shared/preview.css` 与 `../../_shared/preview.js`，仅内联 08 的主题 CSS 与配置，因此可直接以 `file://` 打开，无需生产接口或应用运行时。
