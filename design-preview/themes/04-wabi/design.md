# 04 Wabi — 侘寂留白

## 主题定位

日式禅意极简。该方向围绕“Japanese Zen Minimal”组织，适合需要快速浏览任务状态并频繁切换工作上下文的桌面应用。

## 视觉关键词

大量留白、细如发丝的分割线、一抹朱砂——把工作台变成一间安静的书房。

## 色彩令牌

主色与强调色：`#F7F6F2`、`#1F1D1A`、`#C8442B`、`#15130E`、`#E0724E`。页面同时提供浅色与深色样式，具体令牌如下。

| 模式 | 变量 | 值 |
| --- | --- | --- |
| 亮色 | `--w-bg` | `#F7F6F2` |
| 亮色 | `--w-surface` | `#FFFFFF` |
| 亮色 | `--w-surface2` | `#F1EFE9` |
| 亮色 | `--w-ink` | `#1F1D1A` |
| 亮色 | `--w-mut` | `#8C8780` |
| 亮色 | `--w-line` | `#E0DCD2` |
| 亮色 | `--w-accent` | `#C8442B` |
| 亮色 | `--w-work` | `#5B8C5A` |
| 亮色 | `--w-idle` | `#B5B0A6` |
| 亮色 | `--w-unread` | `#7B6BA8` |
| 亮色 | `--w-error` | `#C8442B` |
| 暗色 | `--w-bg` | `#15130E` |
| 暗色 | `--w-surface` | `#1D1A14` |
| 暗色 | `--w-surface2` | `#252119` |
| 暗色 | `--w-ink` | `#EDEAE2` |
| 暗色 | `--w-mut` | `#8A8478` |
| 暗色 | `--w-line` | `#322E25` |
| 暗色 | `--w-accent` | `#E0724E` |
| 暗色 | `--w-work` | `#8FB579` |
| 暗色 | `--w-idle` | `#6E6A60` |
| 暗色 | `--w-unread` | `#A99BCB` |
| 暗色 | `--w-error` | `#E0724E` |

## 字体与文字

主要字体：`Cormorant`、`Jost`。标题负责建立层级，标签与元数据保持紧凑，以便在任务清单、侧栏和状态区中稳定扫描。

## 布局与组件

预览使用最小高度 `880px` 的舞台，应用窗口宽度为 `632px`。顶栏高度为 `52px`，侧栏宽度为 `300px`；任务列表、筛选项、状态徽标与操作按钮复用同一密度规则。

## 状态与交互

每个页面同时呈现浅色与深色应用窗口。主题切换会更新窗口标题和状态区内容；清单中的选择、复选和标签状态用于展示任务的进行、完成与提醒反馈。

## 可读性与无障碍

正文与背景使用主题定义的前景、边框和强调色令牌保持层级区分。交互元素保留可辨识的轮廓、文本标签与状态色，避免仅依赖单一颜色传达状态。

## React + MUI 实现映射

可用 `CssVarsProvider` 承载令牌；应用框架映射为 `AppBar`、`Drawer` 和 `Box`，列表映射为 `List`/`ListItemButton`，任务行为映射为 `Checkbox`、`Chip`、`IconButton` 与 `Tooltip`。主题的色彩令牌应进入 MUI `palette` 和组件变体，布局尺寸保持为显式设计 token。

## 预览实现

本页引用 `../../_shared/preview.css` 与 `../../_shared/preview.js`，仅内联 04 的主题 CSS 与配置，因此可直接以 `file://` 打开，无需生产接口或应用运行时。
