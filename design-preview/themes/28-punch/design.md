# 28 Punch — 重拳出击

## 主题定位

冲击海报。该方向围绕“Impact Poster Pop”组织，适合需要快速浏览任务状态并频繁切换工作上下文的桌面应用。

## 视觉关键词

重拳出击：最粗的黑框 + 最大的错位投影 + 角度凌厉的超粗标题字，红黄蓝绿四色硬撞——视觉上的一记重拳。

## 色彩令牌

主色与强调色：`#F7F5EE`、`#0A0A0A`、`#FF3D2E`、`#2D5BFF`、`#FFD23F`。页面同时提供浅色与深色样式，具体令牌如下。

| 模式 | 变量 | 值 |
| --- | --- | --- |
| 亮色 | `--pu-bg` | `#F7F5EE` |
| 亮色 | `--pu-surface` | `#FFFFFF` |
| 亮色 | `--pu-surface2` | `#ECE8DA` |
| 亮色 | `--pu-ink` | `#0A0A0A` |
| 亮色 | `--pu-mut` | `#5E5E5E` |
| 亮色 | `--pu-outline` | `#0A0A0A` |
| 亮色 | `--pu-accent` | `#FF3D2E` |
| 亮色 | `--pu-accent2` | `#2D5BFF` |
| 亮色 | `--pu-accent3` | `#FFD23F` |
| 亮色 | `--pu-accent4` | `#16A34A` |
| 亮色 | `--pu-dots` | `rgba(10,10,10,.06)` |
| 亮色 | `--pu-work` | `#16A34A` |
| 亮色 | `--pu-idle` | `#A8A8A8` |
| 亮色 | `--pu-unread` | `#2D5BFF` |
| 亮色 | `--pu-error` | `#FF3D2E` |
| 暗色 | `--pu-bg` | `#0E0E10` |
| 暗色 | `--pu-surface` | `#1A1A1E` |
| 暗色 | `--pu-surface2` | `#242429` |
| 暗色 | `--pu-ink` | `#F2F0E6` |
| 暗色 | `--pu-mut` | `#8E8E94` |
| 暗色 | `--pu-outline` | `#F2F0E6` |
| 暗色 | `--pu-accent` | `#FF5C50` |
| 暗色 | `--pu-accent2` | `#5C82FF` |
| 暗色 | `--pu-accent3` | `#FFDF5C` |
| 暗色 | `--pu-accent4` | `#34D36B` |
| 暗色 | `--pu-dots` | `rgba(242,240,230,.06)` |
| 暗色 | `--pu-work` | `#34D36B` |
| 暗色 | `--pu-idle` | `#4E4E56` |
| 暗色 | `--pu-unread` | `#5C82FF` |
| 暗色 | `--pu-error` | `#FF5C50` |

## 字体与文字

主要字体：`CLIMATE`、`Jakarta`。标题负责建立层级，标签与元数据保持紧凑，以便在任务清单、侧栏和状态区中稳定扫描。

## 布局与组件

预览使用最小高度 `880px` 的舞台，应用窗口宽度为 `632px`。顶栏高度为 `52px`，侧栏宽度为 `300px`；任务列表、筛选项、状态徽标与操作按钮复用同一密度规则。

## 状态与交互

每个页面同时呈现浅色与深色应用窗口。主题切换会更新窗口标题和状态区内容；清单中的选择、复选和标签状态用于展示任务的进行、完成与提醒反馈。

## 可读性与无障碍

正文与背景使用主题定义的前景、边框和强调色令牌保持层级区分。交互元素保留可辨识的轮廓、文本标签与状态色，避免仅依赖单一颜色传达状态。

## React + MUI 实现映射

可用 `CssVarsProvider` 承载令牌；应用框架映射为 `AppBar`、`Drawer` 和 `Box`，列表映射为 `List`/`ListItemButton`，任务行为映射为 `Checkbox`、`Chip`、`IconButton` 与 `Tooltip`。主题的色彩令牌应进入 MUI `palette` 和组件变体，布局尺寸保持为显式设计 token。

## 预览实现

本页引用 `../../_shared/preview.css` 与 `../../_shared/preview.js`，仅内联 28 的主题 CSS 与配置，因此可直接以 `file://` 打开，无需生产接口或应用运行时。

## 家族特性

Pop family 共享高饱和主色、粗体展示字和直接的状态反馈；冲击海报以本页令牌、标题字形和背景处理形成独立辨识度。
